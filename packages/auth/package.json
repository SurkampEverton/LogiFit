{
  "name": "@repo/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Auth — BetterAuth wrapper + LogiFit customizations (magic link, TOTP, WebAuthn, lockout, JWT custom claims). ADR 0092.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server.ts",
    "./client": "./src/client.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/email": "workspace:*",
    "better-auth": "^1.1.1",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "@types/pg": "^8.11.10",
    "tsx": "^4.22.3",
    "typescript": "^5.7.2",
    "vitest": "^2.1.5"
  }
}
