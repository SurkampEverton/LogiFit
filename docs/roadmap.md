# Roadmap LogiFit

Linha do tempo + controle de evolução. Para visão funcional (módulos por área), ver [`modulos.md`](modulos.md).

**Status possíveis:** `todo` · `doing` · `done` · `blocked` · `futuro` (regra 9: 1 `doing` por vez — ver [rules.md](rules.md)).

---

## Fase MVP — Academia + Motor Cross + Comercial + Engajamento + ERP Financeiro + Retenção

| # | Sprint | Funcionalidade | Status | Início | Fim | % | Bloqueios | PR |
|---|---|---|---|---|---|---|---|---|
| 1 | [00](sprints/00-setup-infra.md) | Setup de infra (monorepo, CI, observabilidade, componentes base responsivos) | **done (100% dev)** | 2026-04-27 | 2026-05-22 | 100 | Faixas 1+2+3+4 entregues + GlitchTip SDK plugado (00.Q) 2026-04-27/2026-05-21. Última leva 2026-05-21: scan-upload MVP real (10 magic bytes + embed regex + 28 tests), pino structured logger redact LGPD, 6 componentes UI responsivos (LocaleSwitcher/Breadcrumbs/ResponsiveModal/Table/Form/StickyFooter/CommandPalette ADR 0062-0063), twoConnectionsTest real (T6 ADR 0090), coverage gate em `@repo/errors` 80% + `@repo/ai` 60% (6/9 packages com gate), .githooks/pre-commit sem husky, sbom:generate via CycloneDX, OWASP ZAP weekly workflow, **GlitchTip @sentry/nextjs SDK** (4 configs + `withSentryConfig` + capture hook injetável em `@repo/errors` + runbook `glitchtip-setup.md`). **Pendente:** 3 itens bloqueados por input externo do fundador (backup R2 credentials, DNS security@, HSTS preload submission). | — |
| 1b | [00b](sprints/00b-menu-lateral.md) | **Menu lateral (SideMenu hamburger overlay + registry por módulo + filtro permission/vertical/consent)** | **done** | 2026-05-12 | 2026-05-12 | 100 | Faixas A/B/C/D entregues; E2E Playwright spec ficou em `test.fixme` aguardando auth helper (Sprint 04+) | — |
| 2 | [01a](sprints/01a-identidade-e-topology.md) | Identidade + Topology (groups/tenants/companies/units + RLS raiz) | **done** | 2026-05-12 | 2026-05-12 | 100 | — | — |
| 3 | [01b](sprints/01b-rbac-e-consent.md) | RBAC com scope + grants diretos + Consent LGPD | **done** | 2026-05-12 | 2026-05-13 | 100 | Todas as 5 faixas entregues: A (schemas RBAC + passport + consents + audit hash chain) + B (passaporte cross-tenant) + C (UI registros profissionais) + D.1-D.6 (has_permission/list_user_permissions/cron grants-expired) + Sprint 01b fechamento (compliance schemas: ai_committees CFM 2.454 + privileged_sessions PAM + data_subject_requests LGPD art. 18 + 14 RLS policies + UI /app/settings/roles read-only + UI /app/contador portal read-only + UI /app/settings/compliance/comite-ia + UI /app/settings/privacidade SLA tracker). Sprint 02+ amplia: editor visual de role_permissions + upload PDF ata Comitê + portal self-service /meu/privacidade Sprint 26 | — |
| 4 | [02](sprints/02-geral-crm-pessoas.md) | CRM unificado (members + timeline + dashboard do member) | **done (02a + 02b + 02b2..02b9 + 02c + 02c.4 partials)** | 2026-05-12 | 2026-05-23 | 100 | Faixas A+B+C 2026-05-12 + Path A 2026-05-18 + Sprint 02b backbone 2026-05-19 + **02b2** (ADR 0093 + schema passport_global_identities + signupPatient real) + **02b3** (ADR 0094 + schema passport_global_sessions + helper passport-session + /meu/login 2-tabs + wizard MFA TOTP + cron expire-passport-global-sessions + audit log automático 21 SAs + lint cross-tenant-read-must-log REAL bidirecional) + **02b4** (wrapPassportAction + /meu/perfil 5 SAs + auth-rate-limit 27 unit tests + cron hard-delete-deactivated-passport + ADR 0095 QR code visual server-side) + **02b5** 2026-05-20 (dispatchEmailVerification helper + resendPassportEmailVerification + cron expire-passport-email-verification-tokens daily + change-email + resend-verification E2Es + fix middleware /meu/* aceita passport OR member cookie) + **02b6** 2026-05-20 (requireMemberOrPassport + PassportNeedsLink + 20 pages /meu/* migradas + smoke E2E 24 rotas + split meu-portal-routes passport vs member suites + treino 45/45) + **02b7** 2026-05-20 ([ADR 0098](decisions/0098-feature-flags-mvp-self-host.md) Accepted: feature flags MVP self-host + schema feature_flags + helper isFeatureEnabled cache 60s + 14 flags seedadas + gate signupPatient) + **02b8** 2026-05-20/21 (runbook setup credentials Brevo+Twilio+Turnstile + clarify Twilio não grátis + sendPatientInvite dispatch automático email+WhatsApp+copy-link + [ADR 0099](decisions/0099-whatsapp-nivel-1-wa-me-mvp.md) WhatsApp Nível 1 wa.me + nome profissional no email + UI /invites/new 3 canais + fix(auth) trustedOrigins+@repo/email magic link) + **02b9** 2026-05-21 (/app/passport landing + /app/passport/invites lista filtrável + InviteRowActions copy-link/WhatsApp + form /pessoas/new todos os campos schema PF/PJ + endpoint /api/cep ViaCEP safeFetch regra 37 + fix unrecognized configuration parameter app.tenant_id em 5 INSERTs + fix CNPJ fallback em NOT_FOUND ADR 0048 revisão) + **02c** 2026-05-21 (Faixa D parcial /pessoas/new ConfirmDialog CNPJ inativo + toast.fromApiError regra 45 + revogação inline /invites cancelPatientInvite/revokePatientLink via Confirm/PromptDialog + filtros `?q=` busca nome ILIKE substring + `?module=` 5 opções + `?responsible=` dropdown DISTINCT users hard-cap 200 + paginação cursor base64url {createdAt,id} PAGE_SIZE=25 Carregar mais→ / ←Voltar + drill-down /invites/[id] Server Component header+contato+módulos com responsável JOIN professional_registrations + dataLevels badges + timeline cronológica derivada timestamps + histórico de acessos cross-tenant patient_data_access_log regra 42 hard-cap 50) + **02c.4** 2026-05-23 (schema `patient_link_events` particionado mensal regra 34 + 9 partições 2026-04..2026-12 + RLS append-only enforce em `logifit_app` + 2 enums `passport_event_actor`/`passport_event_kind` (10 kinds canônicos) + helper `logPatientLinkEvent` best-effort no/fora tx + 6 SAs do passaporte emitindo eventos: sendPatientInvite/acceptPatientInvite/cancelPatientInvite/revokePatientLink/confirmModuleSubstitution/setSharingLevel + timeline `/app/passport/invites/[id]` consome events com fallback timestamps pra vínculos pré-migration). Typecheck 12/12 + biome clean. **Sprint 02 fechamento restante**: **02c.5** Redis sliding window real (BLOQUEADO depende Sprint 00 infra); **02c.6** LocaleSwitcher (BLOQUEADO depende sprint i18n migration cross-app); **02d** cron `create-next-partition` mensal pra `patient_link_events` (hoje partições até 2026-12 — janeiro 2027 já bloqueia INSERT) + outbox/retry pra eventos best-effort + emissão member-side em `/meu/privacidade/actions.ts`. | — |
| 5 | [03](sprints/03-geral-agenda-universal.md) | Agenda universal + modalidades Academia | **done** | 2026-05-12 | 2026-05-13 | 100 | Todas as 4 Faixas A+B+C+D entregues (schemas + RLS + EXCLUDE + 9 SAs + 6 rotas UI + ADR 0012 + RRULE expand + widget agenda em member + canvas semanal + Realtime SSE PG NOTIFY); drag&drop full canvas adiado pra Sprint 04+ (não bloqueia) | — |
| 6 | [04](sprints/04-geral-financeiro-asaas.md) | Financeiro Asaas (planos, contratos, cobranças, trancamento, DRE básico) | **done** | 2026-05-13 | 2026-05-13 | 100 | Todas as Faixas A+B+C+D entregues: 6 schemas + 14 RLS + 18 Vitest tests + 7 Server Actions + webhook handler + envelope encryption + 5 rotas UI financeiro + widget financeiro em member detail + job D-5 cobranças + ADRs 0013+0014 | — |
| 7 | [05](sprints/05-geral-ofertas-comerciais.md) | Ofertas comerciais (promoções, pacotes, referrals, cashback) | **done** | 2026-05-13 | 2026-05-13 | 100 | Todas as Faixas A+B+C+D entregues (7 schemas + 20 RLS + 10 tests + 8 Server Actions + canApply validator + UI promoções + widget créditos em member + ADR 0020 + job expiração créditos); seed canônico de 3 promos por tenant adiado pra task pós-Sprint 05 (não bloqueia) | — |
| 8 | [06](sprints/06-geral-copilot-base.md) | **Assistente IA universal base (3 camadas Help/Insight/Action + tool registry distribuído + 7 personas + FAB global + cotas alinhadas a planos — ADR 0064 + 0075)** | **done** | 2026-05-13 | 2026-05-13 | 100 | Todas as 4 Faixas entregues: A (10 schemas IA) + B (seed 8 models + task_routing 7 tasks + 5 schemas RAG + classifier I/O + redact PII + 7 personas pt/en/es + inferPersona + registry + cache + quotas + 61 unit tests verdes + ADR 0015 publicado) + C (Server Actions sendMessage/proposeAction/confirm/reject/switchPersona/saveByokKey/testByokKey/revokeByokKey/saveAssistantName + API Routes /api/ai/chat,session,proposals/[id]/confirm,reject + 9 tools whitelist + 3 ai-blocked + support_tickets + LLM real Vercel AI SDK Gemini default com fallback stub + cota daily real + anti-abuse 10× média 7d) + D (AssistantFAB + AssistantSheet + ActionConfirmDialog + páginas /app/assistente /settings/ia /suporte /super-admin/ai-usage + AssistantNameForm + ByokForm com envelope-crypto AES-256-GCM + i18n 12 arquivos + tenant_assistant_settings schema + lint custom ai-block-respected ativo + RAG seed script (127 docs) + threat model STRIDE v1.0 + RIPD v1.0). Pendências menores adiadas Sprint 06+ próximo PR: hash chain em ai_audit_log (regra 39), prompt_hash computado, job limpeza propostas expiradas, Redis rate limit real (Sprint 00 Faixa 3), cascade fallback runtime, system_prompt_leak classifier, upload UI tenant docs RAG | — |
| 9 | [07](sprints/07-geral-dashboard.md) | Dashboard "Equilíbrio Vital" + cross-alert dispatcher | **done** | 2026-05-13 | 2026-05-13 | 100 | Todas as Faixas entregues: Dashboard `/app` 7 KPIs + alert_subscribers stub + 3 role-aware dashboards (`/recepcao` agenda+checkins+overdue, `/gerente` KPIs+churn recente, `/diretor` MRR+ranking companies+trend 6m com bar chart inline) + Command Palette Ctrl+K 20 items canônicos + filtros `>`/`/` + ADR 0016 tokens "Equilíbrio Vital" publicado (flat design + light/dark via data-theme + anti-shadow). Pendências menores adiadas Sprint 09+: UI `/app/super-admin/database` (ADR 0072), search_index FTS Postgres real com Members/Pessoas (ADR 0062), materialized views `tenant_metrics_daily`, toggle theme persistente `/app/settings/tema` | — |
| 10 | [08](sprints/08-academia-controle-acesso.md) | Controle de acesso Academia (QR HMAC + facial opcional + catraca + bloqueio) | **done** | 2026-05-13 | 2026-05-13 | 100 | Todas as Faixas entregues: schemas + RLS + HMAC QR rotativo + 10 unit tests + 5 SAs + APIs checkin/heartbeat/qr + UI /app/acesso/checkins feed + UI /catracas lista+new wizard + UI /app/members/[id]/qr rotativo com auto-refresh 30s + ADR 0017 (HMAC rotativo) + ADR 0018 (hardware: Android box default). Facial recognition + offline queue PWA + qrcode lib leitor-compatível ficam Sprint 09+ (depende RIPD biometria + decisão de bundle size) | — |
| 11 | [09](sprints/09-geral-engajamento.md) | Engajamento v1 (conquistas + brindes + metas) | **done** | 2026-05-13 | 2026-05-13 | 100 | Todas as Faixas entregues: 6 schemas + 5 enums + 16 RLS policies + DSL `evaluator` Zod-typed com 5 kinds canônicos (checkin_count/payment_streak/goal_reached/tenure_days/referral_count) + 14 unit tests + 2 Server Actions (listMemberAchievements/listMemberGoals) + UI `/app/engajamento` overview com 3 KPIs + widget conquistas + widget metas com progress bar em `/app/members/[id]` + ADR 0021 publicado. Pendências menores adiadas: dispatcher cross-alert real (Sprint 13+ régua), seed canônico 5 conquistas+3 brindes+2 metas (task pós-Sprint), UI catálogo CRUD conquistas/brindes/metas (Sprint 13+) | — |
| 12 | [10](sprints/10-geral-funil-vendas.md) | Funil de vendas (leads, aula experimental, propostas, conversão) | **done** | 2026-05-13 | 2026-05-13 | 100 | Todas as Faixas entregues: A (5 schemas lead_stages/leads/lead_events/trial_classes/proposals + 4 enums + RLS + 8 tests check constraints + append-only) + B (7 Server Actions wrapServerAction: createLead/moveLeadToStage/archiveLead/createProposal/convertLeadToMember atomic transaction com mesmo person_id + listLeads + listLeadStages) + C (UI `/app/vendas` board kanban server-rendered + `/app/vendas/leads` lista tabular + `/app/vendas/leads/new` form quick capture + `/app/vendas/leads/[id]` detalhe com timeline + LeadStageSelector chips + LeadActions converter/arquivar dialogs + seed 6 stages + 10 leads por tenant em 8 tenants) + D (ADR 0022 publicado lead.person_id opcional + quick_* + conversão reusa mesmo person_id). Pendências menores adiadas Sprint 11+: trigger validando person_id em stage 'proposta'/'won', constraint 1 stage won ativo, scheduleTrialClass + appointment is_trial, acceptProposal dispatch, upgradeLeadToPerson wizard, drag-and-drop kanban, E2E completo, feature flag `vendas_v1` | — |
| 13 | [11](sprints/11-geral-prescricoes-e-biblioteca.md) | Prescrições + biblioteca de exercícios com vídeos (workouts, RPE) | **done** | 2026-05-13 | 2026-05-13 | 100 | Todas as Faixas entregues: A (6 schemas treinos: exercises com biblioteca global tenant_id NULL + workouts versionado parent_workout_id + workout_items unique order + prescriptions polimórfico kind+ref_id + workout_sessions + workout_session_items append-only + 16 RLS policies + 13 unit tests) + B (calculateKcalPerSession MET-clássico + 11 tests + 12 Server Actions wrapped: createExercise/listExercises/createWorkout/updateWorkout-versionado/getWorkout/listWorkouts/prescribeWorkout/listMemberPrescriptions/startSession/recordSessionItem/finishSession-preenche-kcal) + C (UI /app/biblioteca/exercicios catálogo + /app/biblioteca/exercicios/new + /app/treinos lista + /app/treinos/new wizard + /app/treinos/[id] detail + /app/members/[id]/treino ficha + /meu/treino/sessao/[sid] execução set-a-set + widget perfil) + D (ADR 0023 publicado prescriptions polimórfico + seed 20 exercícios globais Compendium 2024 + 2 workouts por tenant). Pendências menores adiadas Sprint 11+: cross-prescrição alert (depende getCrossTenantSummary Sprint 02 + meal_plans Sprint 29 + fisio_protocols Sprint 20), upload vídeo MinIO, job cron expira prescriptions, particionamento workout_sessions, drag-and-drop wizard, E2E completo, feature flag treinos_v1, RIPD prescrições, lint cross-tenant-read-must-log | — |
| 14 | [12](sprints/12-geral-avaliacoes-fisicas.md) | Avaliações físicas (bioimpedância, dobras, anamnese, gráficos evolução) | **done** | 2026-05-13 | 2026-05-13 | 100 | Todas as Faixas entregues: A (5 schemas avaliações: assessment_types com biblioteca global tenant_id NULL + fields jsonb declarativo + scoring_method + versionamento via parent_type_id; assessments com type_version snapshot + soft_deleted_at retenção 20a; assessment_measurements value_num/text/enum mutex via check + source pré-cabeada Device Hub Sprint 34; assessment_photos + assessment_calculations cache; 15 RLS policies + 10 unit tests) + B (calculadoras antropométricas: IMC OMS + Pollock 7 dobras Jackson 1980 + TMB Mifflin-St Jeor + TMB Harris-Benedict + TMB Katch-McArdle + RCQ OMS + lean mass; scorer EVA 0-10 Huskisson 1974; 27 unit tests calc + 8 EVA; 7 Server Actions wrapped: createAssessmentType/listAssessmentTypes/createAssessment com cálculos automáticos via transação/listMemberAssessments/getAssessment/compareAssessments/softDeleteAssessment/getLatestAssessmentSummary) + C (UI /app/avaliacoes hub + /app/avaliacoes/tipos catálogo combinado global+tenant com filtros + /app/avaliacoes/tipos/new editor low-code de fields + /app/members/[id]/avaliacoes tabela + /app/members/[id]/avaliacoes/new wizard dinâmico baseado em fields jsonb + context (idade/sexo/altura) pra cálculos + /app/members/[id]/avaliacoes/[id] detail com cálculos derivados + classificação + widget perfil "📊 Última avaliação") + D (ADR 0024 publicado + seed 5 tipos globais: Antropometria Academia + Bioimpedância + Dobras 7 Pollock + Anamnese Academia + EVA Fisio). Pendências menores adiadas Sprint 12+: 7 escalas funcionais restantes (Oswestry/DASH/Tampa/SF-36/Berg/TUG/WOMAC), Petroski/Guedes/Durnin/Faulkner/Cunningham calculadoras, upload photo MinIO+scan, gráficos Recharts, comparação visual lado-a-lado, integração medições com Sprint 09 goals (measurement.recorded), WhatsApp photo-progress (Sprint 13), Device Hub source=device trigger validation (Sprint 34), RIPD avaliações, feature flag avaliacoes_v1 | — |
| 15 | [13](sprints/13-geral-whatsapp-e-regua-cobranca.md) | WhatsApp + régua declarativa (cobrança, confirmação agendamento, estoque) | **done** | 2026-05-13 | 2026-05-13 | 100 | Todas as Faixas entregues (core MVP outbound + DSL régua): A (5 schemas mensagens: message_providers credentials_encrypted + message_templates com approval flow Meta + reguas DSL declarativo trigger/actions/stop_on/guards + regua_executions state machine + messages_sent audit-friendly; 15 RLS + 8 unit tests) + B (DSL Zod `ReguaDslSchema` validando 10 eventos canônicos + 2 kinds ações send_message/wait + 3 helpers nextActionAtFromSteps/renderTemplate/isWithinHourWindow GMT-3 SP + 23 unit tests + 8 Server Actions wrapped: createTemplate/approveTemplate/listTemplates/createRegua/activateRegua/pauseRegua/listReguas/sendMessageManual com stub provider/listMessages/listMemberMessages) + C (UI /app/mensagens hub com 4 cards + /templates lista com badges aprovação + /templates/new com detecção automática de variáveis + /reguas lista com state badges + /reguas/new builder visual com trigger picker + add actions inline + stop_on checkboxes + /historico com filtros canal/status + widget perfil "💬 Mensagens recentes") + D (ADR 0025 accepted provider WhatsApp + ADR 0026 accepted motor DSL + seed 5 templates por tenant: cobranca_d1/d3/d7 + reengajamento_15d + boas_vindas + 1 régua canônica "Cobrança D+1/+3/+7" desativada em 8 tenants = 40 templates + 8 réguas). **Adiado Sprint 13b:** envio real WhatsApp/Email via Twilio/Gupshup/Resend adapter (POC + sub-decisão ADR 0025), evaluator runtime cron tick (consome domain_events + processa regua_executions pending), hub inbound multifluxo ADR 0051 (whatsapp_inbound_messages + identity_matcher + intent_router + classificador IA + scanUpload anexos regra 38), webhook callbacks delivered/read, rate limit Redis, opt-out flow via consents, RIPD whatsapp + DPO sign-off, feature flag mensagens_v1 | — |
| 16 | [14](sprints/14-geral-dre-custos-operacionais.md) | DRE + custos operacionais + previsibilidade + lucratividade por procedimento | **done** | 2026-05-13 | 2026-05-13 | 100 | Todas as Faixas entregues: A (3 schemas custos: cost_categories slug+type fixed/variable + cost_entries amount_cents incurred_at company-scoped + recurring_costs day_of_month 1-28 com check ends_after_starts; 10 RLS policies + 11 unit tests) + B (calculateDre pure function agrupando receita por status/paid_at vs due_at + custos por categoria+type; forecastRevenue heurístico baseline × (1-churn)^N com low/high -15%/+10%; 13 unit tests cobrindo casos canônicos + edge cases; 10 Server Actions wrapped: createCostCategory/listCostCategories/archiveCostCategory/createCostEntry/listCostEntries/deleteCostEntry/createRecurringCost/toggleRecurringCost/listRecurringCosts/generateDre com audit/forecastRevenueAction que calcula baseline contratos ativos × plans.price + churn histórico 6m) + C (UI /app/financeiro/custos lista com filtros + total; /categorias dual-column catálogo+form lateral; /new wizard companyId+categoryId+amount BRL+date+description; /dre com seletor período + 4 KPI cards Receita/Pendente/Custos/Margem + breakdown por categoria com barras horizontais e percentuais; /previsao com 3 KPI cards Baseline/Churn/Total Projetado + tabela mês-a-mês com pessimista/projetado/otimista) + D (seed 6 categorias canônicas Aluguel/Folha/Internet/Marketing/Manutenção/Energia + 10 cost_entries últimos 3 meses + 3 recurring_costs por tenant; 8 tenants total = 48 categorias + 80 custos + 24 recorrências). Pendências menores adiadas Sprint 14+: lucratividade por procedimento via service_type (depende invoice_items backfill Sprint 04+), upload NF-e PDF MinIO + scanUpload regra 38, exportação DRE PDF/CSV via @react-pdf/renderer, simulador interativo sensibilidade com sliders, job recurring-tick diário, permission custos.read/write/dre.read, card dashboard gerente Sprint 07, RIPD custos, feature flag custos_v1, importação extrato OFX/CSV, conciliação bancária, centro de custos por unit, benchmark anonimizado. **ADR não exigido** — categorias com type fixed/variable trivial (Sprint 14 doc confirma) | — |
| 17 | [15](sprints/15-geral-erp-financeiro-core.md) | **ERP Financeiro Core (AP + AR + plano de contas + OCR.space boleto + NF-e XML upload)** | **done (15a core)** | 2026-05-14 | 2026-05-15 | 100 | **Sprint 15a (core MVP) entregue 100%**: 6 schemas + 16 RLS tests + 21 workflow engine tests = 37 tests; 23 Server Actions wrapped (5 chart + 5 supplier + 7 AP + 6 AR); UI completa em 9 rotas (`/plano-contas` tree + `/new` + `/fornecedores` lista+new+detail + `/contas-pagar` lista+new+detail-com-workflow + `/contas-receber` lista+new+detail + `/aging` AP+AR distribuição por faixa); seeds (`seed-plano-contas` ~67 contas brasileiras + 3 approval_rules canônicas; `seed-erp-financeiro` 20 fornecedores + 10 APs + 5 ARs por tenant); ADRs 0033 (plano contas hierárquico) + 0034 (workflow declarativo) **Accepted**. **Sprint 15b (futura, ainda nÃ£o aberta) cobre:** OCR provider multi-vendor (ADR 0035), inbox NF-e unificada `nfe_received`/`nfe_returns` (ADRs 0056/0058), manifestação destinatário (ADR 0057), self-issued NF-e entrada (ADR 0060), retenções tributárias `tax_natures`/`tax_retentions` (ADR 0061), `fiscal_emissions`/`fiscal_events` (ADR 0059 preparação). Schemas AP pré-cabeados sem precisar migration nova | — |
| 18 | [16](sprints/16-geral-rateio-intercompany.md) | **Rateio entre filiais + lançamentos intercompany** | **done** | 2026-05-15 | 2026-05-15 | 100 | Todas as Faixas A+B+C+D entregues: 3 schemas (`allocation_rules` + `ap_allocations` + `intercompany_entries`) + RLS + 2 triggers PL/pgSQL (regra 25 enforcement + auto-flag NF-e) + 11 RLS tests; calculator pure `distribute()` cobrindo 6 kinds (fixed/proportional/per_unit/by_revenue/by_headcount/custom) com 18 unit tests + soma exata garantida; 11 Server Actions wrapped (allocation: create/list/archive/simulate/apply/listForAP + IC: create/liquidate/list/report/balances); UI 6 rotas (`/rateio/regras` + `/new` + `/[id]/simular` simulador interativo + `/intercompany` dashboard com matriz from→to + `/new` + `/fechamento` mensal); ADR 0036 Accepted; seed `seed-rateio-ic` 2 rules + 3 IC entries por tenant owned. **327 tests verdes** (era 298 → +29 Sprint 16) | — |
| 19 | [17](sprints/17-geral-bancos-open-finance.md) | **Bancos + Open Finance + conciliação + automação NF-e SEFAZ** | **done (17a core)** | 2026-05-15 | 2026-05-15 | 100 | **Sprint 17a (core MVP)** entregue: 6 schemas (`bank_accounts`/`openfinance_connections`/`bank_transactions`/`reconciliation_rules`/`company_certificates`/`nfe_sefaz_cursors`) + RLS + 9 RLS tests; 3 libs puras (`ofx-parser`/`reconcile` engine match rules + heurística top-3 / `cashflow-forecast` + validador chave NF-e mod 11) com **30 unit tests**; 14 Server Actions wrapped (bank create/list/archive + importOfx idempotente via FITID + listBankTransactions + confirmMatch + suggestMatchesAction + 3 reconciliation rules + cashflow forecast + Open Finance stub retornando INTERNAL_ERROR até POC); UI 7 rotas (`/bancos` lista + `/new` form com COMMON_BANKS dropdown + `/[id]/extrato` com OFX upload inline + `/[id]/conciliar` cliente Component com loadSuggestions assíncrono mostrando match%+reasons; `/conciliacao/regras` + `/new` editor de condições; `/fluxo-caixa` projeção 30/60/90d com tabela diária color-coded + alerta saldo negativo). ADRs 0037 (Open Finance Pluggy/Belvo) + 0038 (NF-e recepção Arquivei/Sieg/Focus) **Proposed** — promove pra Accepted quando POC com credenciais validar. Seed `seed-bancos` 2 contas + 20 OFX-style tx + 5 reconciliation rules canônicas por tenant. **Sprint 17b futuro (sem credenciais MVP):** OAuth providers reais (Pluggy/Belvo), download NF-e SEFAZ real (Arquivei/Sieg/Focus), upload certificado A1 cifrado, manifestação destinatário UI (ADR 0057), devolução compra (ADR 0058), NFs relacionadas (ADR 0060) | — |
| 20 | [18](sprints/18-geral-adquirencia.md) | **Adquirência (maquininhas Cielo/Stone/Rede/GetNet/PagSeguro + split + antecipação)** | **done (18a core)** | 2026-05-17 | 2026-05-17 | 100 | **Sprint 18a (core MVP)** entregue: 4 schemas (`acquirer_connections`/`acquirer_sales`/`anticipations`/`acquirer_reconciliation_rules`) + RLS + 14 RLS tests; 3 libs puras (`provider` com `MockAcquirerProvider` determinístico + tabela MDR Cielo/Stone/Rede/GetNet/PagSeguro / `fees` com `computeSaleCost`+`quoteAnticipation`+`splitFranchiseSale` consumindo `franchise_agreements` Sprint 01b / `reconcile` heurística top-3 score 55%valor+35%data+10%desc + `detectDivergences`) com **39 unit tests**; 12 Server Actions wrapped (connectAcquirer/test/list/archive + syncAcquirerSales idempotente via NSU + listAcquirerSales + requestAnticipationAction + reconcileSale + suggestSettlementMatchesAction + 3 rules + getUnifiedRevenue); UI 8 rotas (`/adquirencia` lista cards + KPIs 30d + `/new` form mock+sandbox + `/[id]/vendas` extrato com sync button date-range + `/[id]/antecipacao` simulador interativo + histórico + `/conciliacao` cliente com top-3 sugestões color-coded + `/regras` + `/regras/new` editor condições + `/receita` dashboard online+presencial com quebra por provider+top companies). ADR 0039 Proposed (provider abstrato + ordem Stone→Cielo→Rede→GetNet→PagSeguro + antecipação manual default + split runtime). Seed canônico `seed-adquirencia` 2 conexões mock/tenant + 15 vendas/conexão (60 dias dispersos) + 3 rules/tenant = 14 conexões + 210 vendas + 21 rules em 7 tenants. **419 tests verdes** (era 327 → +92 Sprint 18: 14 RLS + 39 unit + 39 outros). **Sprint 18b futuro (sem credenciais MVP):** adapters reais (Stone/Cielo/Rede/GetNet/PagSeguro) + envelope encryption credentials AES-256-GCM (ADR 0073) + webhook callbacks chargeback/cancel + job daily sync + alerta divergência D+2 + antecipação automática por regra + split materializado em `intercompany_entries` cron + RIPD adquirência + feature flag `adquirencia_v1` + E2E sandbox | — |
| 21 | [19](sprints/19-ia-previsao-churn.md) | IA preditiva de churn + intervenções de retenção | **done** | 2026-05-17 | 2026-05-17 | 100 | **Fase 1 entregue 100%** — Família A heurística determinística (40%absence + 30%frequency_drop + 20%overdue + 10%downgrade + atenuadores engajamento/loyalty) com wrapper `predictChurn(features, llmClassifyFn?)` LLM-ready Gemini Flash (caller injeta callback quando habilitado em env; fallback automático pra heurística se Zod schema do output LLM falhar — ADR 0027 §Fase 1 + ADR 0064). 4 schemas (`churn_features_snapshot` append-only + `churn_predictions` com check probs [0,1] + `churn_interventions` lifecycle assigned→closed + `churn_events` unique por member) + RLS + 10 RLS tests; 23 unit tests (10 features + 13 predict); 6 Server Actions wrapped (scorePredict com cache via snapshot_hash + listAtRiskMembers DISTINCT ON + assignIntervention + closeIntervention + feedbackCancellation com auto-link prob_at_churn/was_predicted + getModelStats com recall + intervention success rate); UI 4 rotas (`/retencao` home com 5 KPIs + top 30 em risco color-coded + `/member/[id]` com fatores narrativos + features expandíveis + atribuir/encerrar intervenção + histórico predições + `/interventions` com filtros open/closed/all + `/model` com metadados + recall + latência por versão); Command Palette ganha `nav-retencao` + `nav-retencao-intv`; ADR 0027 promovido **Accepted**; seed `seed-retencao` 10 perfis canônicos (estável/engajado/caindo/atrasado/risco_alto/sumiu/novo/veterano/downgrade/estagnado) × 7 tenants = 70 members + 70 predições (28 low + 28 medium + 14 high) + 4 intervenções amostra. **452 tests verdes** (era 419 → +33 Sprint 19: 10 RLS + 23 unit). **🎉 MVP FECHADO OFICIALMENTE** — 21 sprints entregues, ~6-8 meses de dev solo do Sprint 00 até aqui. **Pendências Sprint 19+ (rolling pós-piloto):** job cron daily recompute, integração com Sprint 13 régua reengajamento_risco_alto, widget `risco` no dashboard do member, permissions retencao.read/write/intervene, feature flag `churn_v1`, E2E completo, integração `resolveModelForTask('classification')` real com Gemini callback | — |
| ~~22~~ | ~~19b~~ | ~~Migração hospedagem Vercel+Supabase → Oracle (ADR 0078)~~ | **deletado** | — | — | — | **superseded por [ADR 0091](decisions/0091-self-host-total-oracle-sp.md)** — self-host total desde Sprint 00; não há migração futura | — |

**MVP fechado oficialmente em 2026-05-17** com Sprint 19 ✅. 21 sprints entregues (00, 00b, 01a, 01b, 02-19). ~3 semanas de execução acelerada (entre 2026-04-27 e 2026-05-17) — todo o backlog do MVP (Academia + Cross + Comercial + Engajamento + ERP Financeiro + Retenção) saiu em estado funcional com 452 unit/RLS tests verdes. Fase 2 (Fisioterapia + ERP Saúde) inicia com Sprint 20 (Prontuário CFM/COFFITO + assinatura ICP-Brasil). Sprint 19b foi **deletado** após [ADR 0091](decisions/0091-self-host-total-oracle-sp.md) (2026-04-27) que pula a fase Vercel+Supabase inteira — desde Sprint 00 a infra é self-host total Oracle Cloud SP + Coolify + Postgres + Caddy + GlitchTip + Loki/Grafana + MinIO. Sprint 00 ganha ~1 semana extra (passa de 4 → 5 semanas) pra absorver o bootstrap Oracle + Coolify + observabilidade self-host.

**\*Sprint 00 status "done\*"** — Camada 2 do backup R2 off-site (regra 40 completa) ficou em standby até fundador fornecer credentials Cloudflare/GPG; Camada 1 local cobre ~80% dos cenários de DR. Reabrir como hot-fix quando ativar.

---

## Fase 2 — Fisioterapia + ERP Saúde

**Fase 2 fechada oficialmente em 2026-05-18** com Sprint 28 ✅. 9 sprints entregues (20-28 + 25a). Prontuário Fisio + Evolução + TISS/TUSS + RH/Comissões + Estoque + ANVISA/CNES + Portal Paciente PWA + Cross-alert lesão Fisio→Academia + Generative UI todos em estado funcional. Fase 3 (Nutrição + Mobile + Fiscal) começa com Sprint 29 (banco de alimentos TACO + plano alimentar interativo).

| # | Sprint | Funcionalidade | Status | Dependências |
|---|---|---|---|---|
| 22 | [20](sprints/20-fisio-prontuario-cid-cif.md) | Prontuário eletrônico COFFITO + CID-11/CIF + assinatura ICP-Brasil + templates | **done (20a core)** | 2026-05-17 | 2026-05-17 | 100 | **Sprint 20a (core MVP Fase 2)** entregue: 8 schemas (cid_catalog/cif_catalog/signature_policies/tenant_signature_overrides + consultas polimórfica com check signed+locked consistente + consulta_cids/cifs + consulta_correction_notes append-only) + RLS (CID/CIF/policies read-all global, consultas tenant-scoped, RLS via JOIN para cids/cifs) + 12 RLS tests; 2 libs puras (signature.ts com resolveSignaturePolicy override-só-endurece + validateLockAttempt gate ADR 0055 + hashConsultaContent canônico sha256 + validateCidCode/validateCifCode/validateCifQualifier) com 27 unit tests; 12 Server Actions wrapped (createConsulta com signature_mode resolvido por kind / updateConsultaContent só-draft / linkCid valida catálogo + status / linkCif valida component+qualifier / lockConsulta com gate professional_registrations CRM/CREFITO/CRN/CREF + MFA recente + hash conteúdo+CIDs+CIFs + councilSnapshot persistido / createCorrectionNote append-only com hash / listConsultasByMember / listCidCatalog ILIKE / listCifCatalog filtros componente / exportPdfStub); UI 6 rotas (`/fisio/pacientes/[id]/prontuario` lista + create button picker kind / `/fisio/consultas/[id]` editor SOAP + CID/CIF picker + lock form + correction notes / `/fisio/consultas/[id]/pdf` preview HTML PDF-like com hash+council rodapé / `/catalogos/cid` busca read-only / `/catalogos/cif` busca + filtro componente); ADR 0028 Proposed (CID/CIF catálogos globais curados LogiFit) + ADR 0032 Accepted já existente; seed `seed-fisio` 5 signature_policies + 51 CIDs top fisio/clínica + 30 CIFs (b/s/d/e). **491 tests verdes** (era 452, +39 Sprint 20: 12 RLS + 27 unit). Migration `0026_silly_doctor_strange.sql`. **Sprint 20b futuro:** ICP-Brasil real (Cert.Sign/Bry/Vaultsign — ADR 0041), @react-pdf/renderer + carimbo ICP, audit em LEITURA de consulta signed, templates por especialidade reusando assessment_types Sprint 12, widget prontuario no member, integração handler `receipt` WhatsApp inbound (ADR 0051), particionamento manual `consultas` (regra 34 / ADR 0072), cold storage Parquet pós-2a, RIPD prontuario v1.0 com DPO sign-off, feature flag `fisio_prontuario_v1`, E2E completo CRM/CREFITO/CRN gates | — |
| 23 | [21](sprints/21-fisio-evolucao-midias.md) | Evolução por sessão SOAP + anexos categorizados em Storage criptografado | **done (21a core)** | 2026-05-17 | 2026-05-17 | 100 | **Sprint 21a (core MVP)** entregue: 2 schemas (evolucoes_sessao @volume 52M+/ano top-5 + evolucao_attachments com 5 kinds + 4 scan_status + CHECK 50MB max) + RLS + 10 RLS tests; lib pura `evolucoes/soap.ts` (validateSoapForLock min 10 chars / validateAttachmentUpload com MIME+size por kind / hashEvolucaoContent canônico SHA-256 / generateStoragePath sanitizado) com 22 unit tests; 9 Server Actions wrapped (createEvolucao com unique appointment / updateEvolucao só-draft / lockEvolucao com hash + transição draft→locked|signed / addAttachmentMetadata com validação dupla / markAttachmentScanResult / softDeleteAttachment / listEvolucoesByMember com count anexos / listEvolucaoAttachments / getAttachmentSignedUrl stub); UI 4 rotas (`/fisio/pacientes/[id]/evolucoes` lista + criar / `/fisio/evolucoes/new?memberId=X` form SOAP completo / `/fisio/evolucoes/[id]` editor + add attachment com browser hash SHA-256 + lock form / `/fisio/pacientes/[id]/timeline-evolucao` visual com dots color-coded por status); seed `seed-evolucoes` 3 evoluções fisio realistas (dor lombar evolutiva 15d/8d/hoje) + 1 anexo foto postural. **523 tests verdes** (era 491, +32 Sprint 21: 10 RLS + 22 unit). Migration `0027_cool_beyonder.sql`. **Sprint 21b futuro:** API Route upload multipart real + scanUpload encadeado regra 38 + URL assinada via MinIO adapter TTL 10min + player vídeo inline + viewer imagem zoom/pan + transcrição Whisper áudio anamnese + comparação fotos antes/depois + search_index sensitive evolucao + RIPD evolucao-midias + DPO sign-off + feature flag `fisio_evolucao_v1` + E2E URL TTL expira | — |
| 24 | [22](sprints/22-fisio-tiss-tuss-convenios.md) | TISS/TUSS + convênios (ANS) + guias XML + glosas | **done (22a core)** 2026-05-17 | 11 schemas + RLS + 10 RLS tests; 3 libs puras (gerador XML TISS 4.01 + validador proativo 10 regras + parser retorno) com 30 unit tests; 14 Server Actions (CRUD + generateGuide com gate ADR 0055 + validador + snapshot + createBatch + processReturnXml + fileGlosa); UI 6 rotas (convenios/autorizacoes/faturamento+detail+/glosas com KPIs); ADRs 0029/0030/0031 Proposed (detalham 0079); seed 5 planos (Unimed/Bradesco/Amil/SulAm/NotreDame) + 28 TUSS top fisio/clínica + import audit 2026.01. **563 tests verdes** (+40). Migration 0028. Sprint 22b: XSD ANS via libxmljs, XMLDSig, ADR 0042 SOAP, OAuth ops, OCR carteirinha, RIPD, feature flag. |
| 25 | [23](sprints/23-fisio-comissoes-repasse.md) | Comissões e repasse de profissional (fechamento mensal + transferência) | **done (23a core)** 2026-05-17 | 4 schemas (professional_contracts versionado / commission_rules priority asc / commission_entries @volume 18M+/ano com CHECK net=gross−retention / commission_periods pipeline draft→approved→paid) + RLS + 14 RLS tests; lib pura calculateCommission cobrindo 4 kinds × 4 bases + overrides + vigência com 25 unit tests; 10 Server Actions wrapped (createContract com gate ADR 0055 + createRule + calculateForEvent idempotente + closePeriod agrega + approvePeriod + markPaid + listings + extract stub); UI 4 rotas (/rh hub + /profissionais + /comissoes filtros + /fechamento filtros); ADR 0086 Proposed; seed-rh 21 contratos + 21 rules + 105 entries em 7 tenants. **602 tests verdes** (+39). Migration 0029. Sprint 23b: calculateRetentions ADR 0061, Asaas transfer, holerite PDF, cron mensal, RIPD, feature flag. |
| 26 | [24](sprints/24-geral-estoque.md) | Estoque (descartáveis + revenda) + POS + inventário | **done (24a core)** 2026-05-17 | 4 schemas (stock_items + stock_movements @volume 2.4M+/ano APPEND-ONLY + stock_inventories + stock_inventory_entries) + RLS + 12 RLS tests; lib pura inventory.ts (calculateBalance + custo médio + PEPS FIFO com cogs + detectLowStockCrossing + calculateInventoryAdjustment) com 23 unit tests; 11 Server Actions (CRUD itens + registerEntry/Exit com recálculo custo médio + alert low_stock + sellAtPos POS + inventory pipeline); UI 2 rotas (/estoque hub 5 KPIs + /itens catálogo com saldos low_stock color-coded); ADR 0087 Proposed (PEPS + custo médio por item; SUM não denormalizado; APPEND-ONLY; multi-company MVP); seed-estoque 20 items + 60 movements. **637 tests verdes** (+35). Migration 0030. Sprint 24b: AR via Sprint 15, NFC-e Focus, multi-unit, view materializada, trigger período fechado, RIPD, feature flag. |
| 27 | [25](sprints/25-fisio-anvisa-cnes.md) | ANVISA (equipamentos + manutenção + limpeza) + integração CNES | **done (25a core)** 2026-05-17 | 5 schemas (equipment unique global serial + maintenance com fluxo externo + usage_log @volume 18M+/ano APPEND-ONLY + cleaning_checklists/logs APPEND-ONLY) + RLS + 13 RLS tests; lib pura anvisa.ts (classifyMaintenances urgency overdue/d7/d30/ok + validateChecklist + validateCnesCode 7 dígitos + checkCleaningStatus) com 21 unit tests; 13 Server Actions (CRUD equipment + scheduleMaintenance + completeMaintenance atualiza last_dates + listAttention + recordUsage + checklists CRUD + recordCleaning com validation auto + validateCnesCodeAction); UI 3 rotas (/vigilancia hub 5 KPIs + /equipamentos + /limpeza); sem ADR novo; seed 3 equipamentos + 1 manutenção cada + 1 checklist + 5 logs por tenant. **671 tests verdes** (+34). Migration 0031. Sprint 25b: Datasus CNES API + NF-e 5.915/1.916 + bucket certificados + permissions + RIPD + feature flag. |
| 28 | [26](sprints/26-geral-portal-paciente-web.md) | Portal do paciente web (PWA) — auth, agenda, recibos, vídeos, QR | **done (26a core)** 2026-05-17 | 3 tabelas (member_auth_tokens + member_sessions + member_consents) + RLS + 12 RLS tests; 4 libs puras (magic-link + cancellation-policy + consent + qr-rotation) com 53 unit tests; 9 Server Actions wrapped (requestMagicLink rate-limit + anti-enumeration + verifyMagicLink + logout + cancelMyAppointment + revokeMySession + updateMyConsent + acknowledgeCrossPrescriptionAlert + acknowledgeIncident + exportCrossTenantAccessLog); 14 rotas UI PWA mobile-first (`/meu`, `/meu/login` + `/verify`, `/meu/agenda` + `/novo`, `/meu/financeiro` + `/pagar/[id]` + `/meu/recibos`, `/meu/treino`, `/meu/qr` com refresh 60s via API, `/meu/perfil` + dispositivos + logout, `/meu/privacidade` overview + 4 cross-tenant: compartilhamento/acessos/alertas-cruzados/incidentes, `/meu/convidar`) com 400+ linhas de estilos `.ev-portal-*` em globals.css (safe-area-inset, bottom nav 4 slots, touch ≥44px); 4 API Routes (`POST /api/meu/magic-link` + `/verify`, `GET /api/meu/qr` rotação, `GET /api/meu/privacidade/export/[id]` CSV TTL 7d); PWA manifest dinâmico via Route Handler + service worker mínimo (pre-cache shell + network-first fallback) + RegisterSw client; ADR 0088 Proposed (magic link 15min + cookie opaco path=/meu + sessão 30d + sem MFA member + anti-enumeration); migration `0032_green_marauders.sql`. **736 tests verdes** (era 671, +65 Sprint 26: 12 RLS + 53 unit). **Sprint 26b/c futuro:** booking online direto (SA `bookMyAppointment` reusa Sprint 03 com actor=member), Asaas checkout embed iframe (vez de redirect), @react-pdf/renderer pra recibos + PDF export acessos, schemas `cross_prescription_alerts` (Sprint 11+ extensão) + `security_incidents` (ADR 0067 addendum), autocomplete `/meu/convidar` path C, push notifications VAPID + dispatcher de incidentes/alertas, RIPD v1.0-portal-paciente + DPO sign-off, feature flag `portal_member_v1`, E2E Playwright 3 viewports + Lighthouse PWA ≥95, substituir window.confirm por `<ConfirmDialog>` swipe gestures, refresh token rotativo single-use, SMS Twilio segundo canal, MFA opcional tenant-level, rate limit Redis IP (regra 36 completa) | — |
| 29 | [27](sprints/27-cross-alert-lesao-treino.md) | Cross-alert: lesão Fisio → adaptação automática de workout Academia | **done (27a core)** 2026-05-18 | 3 schemas (`cid_exercise_contraindications` global+tenant override + `member_injury_alerts` com blocked_reason audit + `workout_adaptations` 1:1 alert) + RLS + 16 RLS tests; 2 libs puras (`detectContraindications` matcher 3 níveis precedência exercise>pattern>group + agregação max severity / `canCrossModuleAlert` gates regra 25 first + consent + contrato + workout / `isActionableCid` filtro chapter CID-11 / `buildAdaptationDiff` jsonb canônico avoid+alt→replaced, avoid sem alt→removed, modify+alt→replaced, caution→mantém) com 33 unit tests; 7 Server Actions wrapped (`processInjuryAlert` dispatcher: carrega consulta + CIDs + workout + consent + topology, aplica gates, cria alert pending OU blocked com audit completo + adaptation suggested via lib pura; `suggestWorkoutAdaptation` re-roda manualmente; `confirmAdaptation` materializa new workout versionado parent+1 + atualiza prescription transacional + alert→accepted; `rejectAdaptation`; `listPendingAdaptations` / `listInjuryAlerts` para fila instrutor e gerente); UI 6 rotas (`/app/cross` hub 5 KPIs, `/app/cross/alertas` lista filtrável por status incluindo blocked, `/app/cross/alertas/[id]` detalhe com consent_id_used + companies, `/app/treinos/adaptacao-pendente` fila instrutor com counts avoid/modify/caution, `/app/treinos/adaptacao-pendente/[id]` diff visual color-coded + AdaptationActions client component confirm/reject, `/meu/alertas` portal paciente timeline com status amigável); seed `seed-cross-contras` 35 contraindicações canônicas globais cobrindo top patologias musculoesqueléticas (MG30.0 lombalgia + FB52 manguito + BA00 LCA + FB55.0 patelar + 30 outros) com sources COFFITO 414/2012, ACSM 2023, curadoria LogiFit; ADR 0084 Proposed (tabela curada > IA generativa: determinístico + auditável + 3 níveis granularidade + 3 severidades + catálogo global+tenant override + sempre sugere nunca aplica direto + filtro CID por chapter + 3 cenários E2E documentados); migration `0033_cross_alert_lesao.sql` (3 enums + 3 tabelas + 4 indexes + FKs). **769 tests verdes** (era 736, +33 Sprint 27 unit: 17 contraindications + 16 franchise-gate). **Sprint 27b/c futuro:** IA complementar Gemini Flash via `resolveModelForTask('classification')` quando catálogo não cobre (regra 32 + audit log SaMD); `cid_chapter_actionable` lookup refinada; `movement_pattern` enum dedicado + enriquecimento `exercises.metadata.movement_patterns`; `auto_apply` Enterprise (com termo); listener real `consulta.signed` Sprint 20 → dispatch automático; integração régua Sprint 13 notificar instrutor WhatsApp; cron expirar pending_review > 14d → expired; E2E Playwright (happy + bloqueio regra 25 + bloqueio consent_missing); adaptação cross-prescription Sprint 11; `overrideAdaptation` instrutor edita diff; trocar window.confirm por `<ConfirmDialog>` (regra 45); feature flag `cross_alert_lesao_v1`; stretch notificar nutri + cross-alert inverso (meta atingida → próxima avaliação fisio) | — |
| 30 | [28](sprints/28-fisio-generative-ui.md) | Generative UI v1 (cards de relatório clínico via tool calls) | **done (28a core — fecha Fase 2)** 2026-05-18 | **Zero migration** (toolCalls jsonb já existia Sprint 06); framework `@repo/ai/genui` (registry singleton + 4 guardrails Zod runtime: unknown_tool/schema_violation/persona_not_allowed/mutation_attempted readOnly-only MVP) com 18 unit tests; 6 tools default canônicas registradas (`genui.fisio.patient_card`/`evolution_chart`/`cid_suggestion` apenas professional_clinical regra 28/`exercise_recommendation` integra flag avoid|modify|caution ADR 0084 / `genui.geral.measurement_comparison`/`report_section` markdown leve); 7 componentes React em `@repo/ui/genui` (PatientCard + EvolutionChart SVG nativo com faixa referência + CidSuggestion client com ranking confidence + ExerciseRecommendation barra lateral por flag + MeasurementComparison tabela delta% color-coded + ReportSection markdown leve mini-parser + GenUIMessage renderer com dispatch fixo); Server Action `composeGenUIResponse` wrapped (resolve session + LLM stub determinístico por keywords + valida cada tool call com persona + insert assistant_messages.tool_calls jsonb auditável + retorna blocks); API POST `/api/ai/genui` batch endpoint; UI `/app/copilot/genui-demo` Server Component + client form com 4 prompts pré-feitos via `?q=` + autosubmit + render `<GenUIMessage>` multi-turn; ADR 0085 Proposed (framework próprio sobre assistantMessages.toolCalls vs streamUI > registro estático sempre vs dinâmico > persona-aware obrigatório > batch MVP > guardrails Zod runtime). **787 tests verdes** (era 769 +18 genui). **🎉 Fase 2 fechada** — Fisio Sprint 20-23 + ERP Saúde 24-25 + Portal Paciente 26 + Cross-alert 27 + Generative UI 28 todos done. **Sprint 28b/c futuro:** LLM real via `resolveModelForTask('chat')` Vertex AI Gemini + tool calling; streaming SSE progressivo `/api/ai/genui/stream`; componentes adicionais `<WorkoutCard />`/`<TrainingHistory />` Academia + `<MealPlanCard />`/`<NutritionTable />` Nutri; Recharts substitui SVG nativo com tooltips/zoom; persistência de layout multi-turn; export PDF com layout preservado; few-shot prompts engineering; E2E Playwright happy path resumo→card+chart→clicar CID; feature flag `genui_v1`; markdown sanitizado via remark-parse em ReportSection; `ai_audit_log` row dedicada por tool call com hash chain regra 39 | — |

---

## Fase 3 — Nutrição + Mobile + Fiscal

**Fase 3 aberta oficialmente em 2026-05-18** com Sprint 29 ✅. Backbones entregues 2026-05-18: #29 (Nutri Foods+Planos) + #30 (Nutri Suplementos+Exames) + #31 (Diário+Teleconsulta) + #32 (Device Hub) + #33 (Pipeline Exames Laboratoriais) + **#34 (IA Nutri-Agent)** + #35a (App Expo backbone) + #36a (Fiscal Focus NFe backbone). **8 backbones em um dia** — Fase 3 com todas funcionalidades core entregues como Faixa A. **2026-05-23:** #37 (Apuração mensal Grupo C — ADR 0100 Proposed) entregue como backbone 37a — motor próprio + 24 unit tests + 5 SAs + 2 rotas UI + memorial estruturado. Restam #38-40 (guias + obrigações + folha — pós-produção, avaliar make vs buy) e refinamentos #29b-#37c (LLM real, RIPDs/ANVISA sign-off, particionamentos, feature flags piloto, E2Es).

| # | Sprint | Funcionalidade | Módulo | Status | Dependências |
|---|---|---|---|---|---|
| 31 | [29](sprints/29-nutri-alimentos-e-plano.md) | Banco de alimentos TACO + plano alimentar interativo + substituições + PDF branding | nutri | **done (29a core)** 2026-05-18 | 7 schemas + RLS + 12 RLS tests; 2 libs puras (nutrients-schema Zod strict 30+ campos + faixas fisiológicas + calculateMealPlanNutrition + compareAgainstTargets ±10% gaps + rankEquivalents proximidade calórica) com 26 unit tests; 9 Server Actions wrapped (searchFoods/getFoodDetail/createTenantFood/listMealPlans/createMealPlan transacional + meals + items/getMealPlanFull com JOIN nutrients + nutrition + gaps/updateMealPlan versionado via Sprint 11 pattern/listSubstitutions/upsertBranding/getBranding); UI 7 rotas (`/app/nutri` hub 5 KPIs; `/alimentos` busca form GET + tabela macros; `/alimentos/[id]` detalhe 30+ nutrientes; `/planos` filtrável; `/planos/[id]` 4 KPIs gaps emoji + breakdown items × P/C/L; `/settings/branding` color picker + preview); seed 47 alimentos TACO canônicos + 20 equivalências curadas direcionais; ADR 0080 Proposed (nutrients jsonb Zod strict > tabela 1:N + catálogo global + tenant custom + faixas fisiológicas) + ADR 0081 Proposed (3 níveis + versionamento Sprint 11 pattern + cálculo runtime função pura > materialized view + substituições curadas > IA + targets no plano > contracts). Migration `0034_nutri_foods_planos.sql`. **813 tests verdes** (era 787 +26 unit). **🚀 Fase 3 aberta.** **Sprint 29b/c:** editor drag-drop visual + TACO completa 3000 entries via migration data + auto-targets via TDEE ADR 0070 + cache cached_totals jsonb + 10 planos modelo + PDF render `@react-pdf/renderer` + upload logo MinIO + pg_trgm/unaccent extensions + trigger name_normalized + USDA segundo source + RIPD nutri-plano + DPO sign-off + feature flag `nutri_plano_v1` + widget `<MealPlanCard />` Sprint 28 GenUI integração + `/meu/cardapio` portal Sprint 26 + E2E Playwright + IA Copilot template-gen stretch | MVP + Fase 2 |
| 32 | [30](sprints/30-nutri-suplementos-exames.md) | Suplementação (catálogo + prescrição + interações) + exames laboratoriais + referências + alertas | nutri | **done (30a core)** 2026-05-18 | 6 schemas (supplements global+tenant + supplement_interactions + supplement_prescriptions separado de Sprint 11 + lab_analytes global + lab_reference_ranges 1:N sex×age×condition + lab_results out_of_range denormalizado) + RLS + 13 RLS tests; lib pura `lab.ts` (matchReferenceRange scoring por especificidade + isOutOfRange 4 casos + classifyLabResult com severity mild/severe + ageYearsAt) com 23 unit tests; 11 Server Actions wrapped (search/createTenantSupplement/listInteractions/prescribeSupplement/discontinue/listMember + listLabAnalytes/getWithReferences/registerLabResult com classificação automática via lib pura + listMemberLabResults filtrável + compareAnalyteOverTime); UI 4 rotas (/app/nutri/suplementos lista form GET filtro kind + detail interactions ordenadas por severity com borda colorida + /app/nutri/exames catálogo categoria filter + count ranges); seed `nutri-labs` 10 suplementos canônicos + 30 interações curadas (Mayo/BNF/SBEM/WHO/AHA) + 20 analitos com faixas multi-segmentadas (split sex + gestante + osteoporose etc) sources SBD/SBC/SBPC/SBEM/OMS/SBAC; ADR 0082 Proposed (suplementos separados de foods + supplement_prescriptions separada de Sprint 11 + lab_reference_ranges 1:N > jsonb + out_of_range denormalizado > VIEW + curadoria LogiFit revisão semestral); Migration `0035_nutri_labs_suplementos.sql`. **836 tests verdes** (era 813 +23 lab.ts unit). **Sprint 30b/c:** 50 analitos extras (eletrólitos + hormônios + tumorais + urinálise) + faixas pediátricas/geriátricas + páginas /app/members/[id]/exames + suplementação + OCR laudo PDF (Sprint 33 cobre) + comparação cross-lab + tenant override ranges + cron valida out_of_range integridade + particionamento anual lab_results + régua Sprint 13 alerta WhatsApp + widget GenUI MealPlanCard exames alterados + portal /meu/exames Sprint 26 + RIPD exames-laboratoriais + DPO + feature flag `nutri_suplementos_exames_v1` + E2E + IA nutri-agent consulta evolução analitos | #31 (Sprint 29), #23 (Sprint 11) |
| 33 | [31](sprints/31-geral-diario-alimentar-teleconsulta.md) | Diário alimentar (paciente + validação nutri) + teleconsulta (vídeo com provider abstrato) | geral/nutri | **done (31a core)** 2026-05-18 | 4 schemas (`meal_log_entries` particionamento mensal Sprint 31b + check has_content + review_status cache + `food_log_daily_summary` PK tenant×member×date alimenta calculateCaloricBalance ADR 0070 + `meal_log_reviews` 1:N staff-only + `teleconsultation_sessions` provider enum + recording/transcription consents separados LGPD art. 11 + checks ended_consistency/recording_requires_consent/transcript_requires_consent); RLS + 12 RLS tests; lib pura `diario/calc.ts` (calculateDailyDiarySummary + calculateAdherence threshold ≥80% gramas + detectMealDeviation ±15% tolerância + detectAllMealDeviations ordenado por \|delta\|) com 17 unit tests; lib pura `@repo/ai/teleconsulta` provider abstrato (TeleconsultaProvider interface + MockTeleconsultaProvider dev/test + resolveTeleconsultaProvider resolver — Daily/Whereby/Jitsi/Twilio em Sprint 31b); 10 Server Actions wrapped (member portal: logMeal Zod runtime + resolve nutrients foods + calcula calculated_nutrition jsonb + resolve meal_plan ativo + listMyDiary + getDiaryEntry + deleteDiaryEntry; nutri staff: listMemberDiary + validateMealEntry transação review + cache + listPendingReviews fila + generateDiaryReport summary fallback live; teleconsulta: scheduleTeleconsultation com provider.createRoom + startTeleconsultation idempotente + endTeleconsultation best-effort + listTeleconsultations + acceptRecordingConsent + acceptTranscriptionConsent granulares); UI 3 rotas (`/meu/diario` lista 14 dias agrupado por data + total kcal + badges review status; `/meu/diario/novo` form mobile-first; `/app/nutri/diario` fila pendente review ORDER BY created_at + member name + idade entry); ADR 0083 Proposed (provider abstrato + Daily.co default + cliente embed iframe > SDK + gravação MinIO próprio > storage provider + transcrição pós-gravação Groq Whisper > ao vivo + 2 consents distintos + sem failover multi-provider + POC plan documentado Sprint 31b); Migration `0036_diario_teleconsulta.sql`. **853 tests verdes** (era 836 +17 diário unit). **Sprint 31b/c:** POC Daily real + DPA + UI canvas vídeo iframe + chat realtime + webhook gravação/transcrição + job Groq Whisper transcript jsonb turnos + rascunho SOAP agent soap_drafter ADR 0085 GenUI + tenant_settings provider override + reminder pré-consulta régua Sprint 13 + picker foods autocomplete + upload foto MinIO scanUpload + detalhe /app/members/[id]/diario aprovação rápida inline + relatório PDF @react-pdf/renderer + cron aggregate-diary-daily-summary 02:00 SP + integração régua meal.reviewed/diário-parado + conquistas Sprint 09 5refeições/sem + widget perfil diario + RIPDs nutri-diario + teleconsulta + DPO sign-off + feature flags `diario_v1`/`teleconsulta_v1`/`teleconsulta_stt_v1` + E2E Playwright + IA reconhecimento foto stretch + diário por voz Whisper stretch | #28, #31, #22 |
| 34 | [32](sprints/32-geral-device-hub.md) | **Device Hub v1 — wearables (Garmin, Oura) + BLE bioimpedância + import FIT/CSV + curadoria profissional + monitoramento + alertas + timeline enriquecida** | geral | **done (32a core)** 2026-05-18 | 7 schemas (device_connections OAuth+BLE unique active + device_readings FHIR-like Observation @180M+/ano partição diária Sprint 32b retenção raw 90d + device_readings_daily_summary perpétuo PK+check min≤max + device_readings_curated snapshot pre-drop pra rastreabilidade Lei 13.787 + device_sync_cursors + device_consents granular 1 ativo por (member,provider) + raw_data_access flag separado + device_incidents audit) + RLS + 10 RLS tests; 3 libs puras em `@repo/ai/devices` (DeviceProvider interface + MockProvider gera 21 readings determinísticas + resolveDeviceProvider; normalizer com PHYSIOLOGICAL_RANGES 18 codes + validateReading + partitionValidReadings + aggregateDailySummaries + detectOutliers z-score ±3σ; parseInBodyCsv tolerante BR+US formatos) com 17 unit tests; 8 Server Actions wrapped member portal (startConnection com state OAuth + completeConnection valida state + provider.completeAuth + grava tokens + disconnect provider.revoke best-effort + listMyConnections + listMyReadings filtrável + importInBodyCsv parse+partition+INSERT ON CONFLICT dedup + grantDeviceConsent revoga anterior + revokeDeviceConsent); UI 4 rotas portal Sprint 26 (`/meu/dispositivos` lista conectados + disponíveis com badge Sprint 32b pros OAuth e file_import funcional; `/meu/dispositivos/historico` últimas 200 leituras 90d com label legível; `/meu/dispositivos/importar` page+form CSV InBody com preview erros parse + contadores válido/inválido/inserido); ADR 0049 Proposed (provider abstrato > hard-code + FHIR-like Observation > schema proprietário + retenção 3 camadas raw 90d → agregado perpétuo → curadas perpétuas + curadoria profissional obrigatória NUNCA automação regra 28 + consent granular por provider + separação visual device 📱 vs avaliação 🩺 sem calibração automática cross-fonte + particionamento diário com aggregate-then-drop em sequência Sprint 32b + 8 alternativas rejeitadas documentadas); Migration `0037_device_hub.sql`. **870 tests verdes** (era 853 +17 devices unit). **Sprint 32b/c:** adapters reais Garmin Connect/Oura Ring/Fitbit Web API com safeFetch regra 37 + allowlist hosts + Web Bluetooth Omron HBF-226 GATT 0x181D + G-Tech + parsers FIT/TCX/GPX + particionamento diário device_readings + jobs aggregate-daily-summaries (02:00 SP) → drop-old-partitions (00:00 UTC) dependência ordem garantida + envelope encryption tokens via LOGIFIT_DATA_KEY ADR 0073 + UI `/app/members/[id]/dispositivos/curar` curadoria lado-a-lado + `/app/members/[id]/monitoramento` painel tendências Uso 2 + cross-alert Sprint 07 consome leituras (HR+10bpm 3d → fisio; passos <3k 7d → instrutor) + `assessment_measurements.source_device_reading_id` FK + UI Sprint 12 + member_device_connections search_index regra 30 + permissions devices.read/read_raw/admin + RIPD device-hub + DPO + feature flag `device_hub_v1` + E2E Playwright + webhook push Garmin + dashboard saúde populacional anonimizado + Apple Health + Google Health Connect via app nativo Expo Sprint 35 | #26 portal + #13 régua + #07 cross-alert |
| 35 | [33](sprints/33-geral-pipeline-exames.md) | **Pipeline Inteligente de Exames Laboratoriais — upload PDF → OCR → IA extração + IA interpretação conservadora → revisão profissional → lab_results oficial; paciente pode subir via portal** | geral | **done (33a core)** 2026-05-18 | 6 schemas (`exam_documents` particionável anual Sprint 33b @volume 2M+/ano retenção 20a Lei 13.787 com 5 indexes incl. partial pendentes+sensitive + 2 CHECKs uploader/review consistency + `exam_extractions` 1:1 com structured_data Zod-strict + CHECK confidence 0-1 + `exam_interpretations_draft` com blocked_by_classifier flag + classifier_blocked_terms jsonb audit + `exam_interpretations_final` revisada + rejected_hypotheses audit + `exam_review_edits` append-only sem UPDATE/DELETE policy + `tenant_exam_ai_settings` opt-out + classifier_strictness strict/moderate) + RLS FORCE em todas 6 (`0051_exames_rls.sql` com 10 policies tenant scope + member portal `app.member_id` access em select de documents e final + GRANT diferenciado pra append-only); 3 libs puras `@repo/ai/exames` (`classifier.ts` 113l com 11 patterns STRICT diagnóstico/prescrição + 5 MODERATE confiança excessiva + classifyInterpretationFields agrega multi-string + getBlockedMessage UI-friendly; `extraction-schema.ts` 69l Zod `.strict()` ExamAnalyteSchema + ExamExtractionSchema + parseExtractionJson + safeParseExtractionJson; `interpretation.ts` 337l compareWithRanges scoring condition>sex>age + classifyOutOfRange severity threshold 20% + detectPatterns 7 catálogo curado: perfil_aterogenico/anemia_ferropriva/resistencia_insulina/disfunção_hepática/hipotireoidismo/deficiência_D/deficiência_B12 + getFollowUpSuggestions deduplicado por padrão) com 36 unit tests verdes (19 classifier + 17 interpretation); 9 Server Actions wrapped (staff 685l: uploadExamDocument + processExam stub OCR+IA determinístico cobrindo pipeline real Sprint 33b + submitExamReview transacional cria final+audit+lab_results Sprint 30 + listPendingExams ASC + getExamDetail + markSensitive + rejectExam; member portal 90l: selfUploadExam patient_portal + listMyExams via withMemberContext); 5 rotas UI mínimas (`/app/exames/fila` lista pendentes com 7 colunas + ícones origem 👨‍⚕️/📱/💬/🔌 + Status IA color-coded; `/app/exames/[id]` detalhe read-only PDF metadata + tabela analitos com badge ⬆/⬇ severity + cards padrões confidence% + follow-up + banner amarelo blocked terms; `/app/nutri/exames` nutri-específica reusa backbone; `/meu/exames` portal mobile-first status badge amigável; `/meu/exames/upload` placeholder Sprint 33b); 8 RLS+check tests em `exames-rls.test.ts` (insert válido + uploader_consistency NULL rejeita 23514 + review_consistency status=published sem reviewed_at rejeita + patient_portal OK uploaded_by_member_id + isolation cross-tenant + confidence_range 1.5 rejeita + blocked_by_classifier preserva texto jsonb + opt-out insert+update + isolation tenant_exam_ai_settings); ADR 0050 já Accepted 2026-04-23. Migration `0038_exames_pipeline.sql`. **914 tests verdes** (era 870 +44 Sprint 33: 36 unit + 8 RLS). **Sprint 33b/c futuro:** `resolveModelForTask('extraction')` Vertex AI Gemini real regra 32 + OCR provider abstrato Sprint 15 ADR 0035 com safeFetch allowlist + scanUpload regra 38 PDF JS detection + MinIO bucket lab-documents cifrado + URL assinada TTL 10min + UI drag-drop members upload + **table editor lado-a-lado completo** PDF embed + analytes editable + cards hipóteses confirmar/rejeitar individual + particionamento ANUAL retenção 20a + cold storage 1hot+19cold + permissions exam.read/write/review/sensitive.read + consent self_upload_exam + integração régua Sprint 13 valores críticos + handler exam-upload hub WhatsApp inbound Sprint 13 ADR 0051 + UI super-admin opt-out + classifier_strictness + pesquisa global ADR 0062 is_sensitive=true + **notificação ANVISA RDC 657/2022 SaMD Classe II** regra 28 + **RIPD v1.0-exames-laboratoriais** DPO sign-off regra 29 + feature flag exames_ia_v1 + E2E Playwright fluxo profissional+paciente + classifier 100% benchmark | #06 copilot + #15 OCR + #30 exames + #26 portal |
| 36 | [34](sprints/34-nutri-agent-ia.md) | IA Nutri-Agent cruzando log Academia + prontuário Fisio + diário alimentar + antropometria + **device_readings (Device Hub)** + **lab_results (Pipeline Exames)** | nutri/ia | **done (34a core)** 2026-05-18 | 3 schemas (`nutri_agent_runs` 6 status + 4 triggers + 2 indexes incl. partial fila ativa + CHECK completed_consistency / `nutri_agent_suggestions` 5 kinds plan_adjustment/alert/risk_pattern/pre_consult_summary/follow_up_exam + 3 severities + 4 statuses + targetMealPlanId/appliedMealPlanId FKs setNull Sprint 29 + blocked_by_classifier flag reusa Sprint 33 + expiresAt 14d + 4 indexes incl. partial pending+severity + 2 CHECKs confidence_range 0-1 e reviewed_consistency / `nutri_agent_metrics_snapshot` append-only com data jsonb cross-module + data_hash SHA-256 reprodutibilidade + 2 indexes run+hash) + RLS `0052_nutri_agent_rls.sql` FORCE em 3 + 8 policies tenant scope + member portal `app.member_id` em suggestions accepted + GRANT diferenciado metrics append-only; 3 libs puras `@repo/ai/nutri-agent` (508l) — types.ts MemberContextSnapshot canônico cross-module Sprints 29/30/31/32/33; **pattern-detector.ts** 7 detectores curados conservadores (estratégia catálogo > IA generativa): `deficit_calorico_extremo` avg_kcal/target<0.7 attention <0.5 critical conf 0.95 / `aderencia_baixa` adherence<50% info <30% attention conf 0.92 / `overtraining_sugestivo` HR>75bpm + sleep<360min concomitantes conf 0.78 / `risco_cardiovascular_lipidico` LDL above + HDL below outOfRange conf 0.88 / `risco_glicemico` glicose_jejum/hba1c above outOfRange conf 0.86 / `perda_peso_rapida` trend<-6kg/month conf 0.90 / `fisio_workout_tensao` CIDs ativos + ≥5 sessões conf 0.75 + orchestrator detectRiskPatterns ordena por severityRank; **suggestion-generator.ts** generateSuggestionsFromPatterns com PATTERN_TO_KIND mapping + computeProposedChanges conservador (deficit: target ×1.1; weight_loss: +200kcal; outros sem changes profissional decide) + generatePreConsultSummary determinístico confidence 1.0 com bullet list plano+diary+CIDs+exames+padrões; 18 unit tests verdes pattern-detector cobrindo thresholds + concomitância + orchestrator order; 5 Server Actions wrapped (`runNutriAgentForMember` orquestra 7 etapas: **gate Comitê IA regra 13/28** valida `ai_committees.status='active'` → blocked+FORBIDDEN se inativo + cria run collecting → collectMemberContext cross-module (persons+mealPlans+food_log_daily_summary+lab_results+labAnalytes+consulta_cids fisio signed+device_readings_daily_summary 7d agregado+prescriptions workout) → metrics_snapshot data+hash → analyzing → detectRiskPatterns+generateSuggestionsFromPatterns+generatePreConsultSummary → classifier guard regra 28 reusa Sprint 33 → persiste suggestions blocked_by_classifier flag + expiresAt 14d → completed com summary jsonb; erro→failed; `listSuggestions` filtros JOIN member name ORDER BY expiresAt ASC; `acceptSuggestion` MVP só registra (Sprint 34b conecta applyChanges→updateMealPlan); `rejectSuggestion` com reason; `getPreConsultSummary` retorna kind=pre_consult_summary mais recente); UI `/app/nutri-agent` dashboard 224l Server Component 5 KPI cards (Pendentes/Críticas vermelho/Aceitas 30d/Runs 30d/Bloqueadas Comitê IA 30d amarelo) + nav filtros severity (Todas/critical/attention/info) + lista cards color-coded severity + KIND_LABEL 🍽/⚠/🩺/📋/🧪 + confidence% + expira + member name link; 9 RLS tests `nutri-agent-rls.test.ts` (264l); 2 ADRs Proposed 2026-05-18 — [ADR 0043](decisions/0043-nutri-agent-arquitetura.md) agente especializado vs Copilot generalizado + [ADR 0044](decisions/0044-nutri-agent-politica-mudancas-plano.md) política sempre proposta nunca write direto + revisão profissional obrigatória; migration `0039_nutri_agent.sql`. **941 tests verdes** (era 914 +27 Sprint 34: 18 unit + 9 RLS). **Sprint 34b/c futuro:** IA real `resolveModelForTask('reasoning')` Vertex AI Gemini Pro substitui pattern-detector determinístico em padrões não-catalogados + cache semântico via data_hash + cron triggers Vercel Cron (pre_consult_auto 24h + weekly_adherence domingo 06:00 + risk_event_triggered consumer domain_events Sprint 31) + `acceptSuggestion(applyChanges=true)` integra updateMealPlan Sprint 29 versionado + lint custom `no-nutri-agent-direct-write` + cron `expire-nutri-agent-suggestions` D+14 + UI accept/reject inline + diff visual proposedChanges + `/app/members/[id]/nutri-summary` consolidado timeline runs + particionamento ANUAL `nutri_agent_runs` (regra 34 @volume 720k+/ano) + permissions RBAC + consent `cross_module_nutri_agent` + **notificação ANVISA RDC 657/2022 SaMD Classe II** regra 28 + **RIPD v1.0-nutri-agent-ia** DPO sign-off regra 29 + feature flag `nutri_agent_v1` + E2E Playwright fluxo manual + cron + gate Comitê IA bloqueia + piloto ≥10 runs reais antes de promover ADRs Accepted + stretch: tool calling LLM via tools_registry ADR 0075 + persona nutricionista-agent + RAG SBNutri/ABRAN + reconhecimento foto refeição Whisper-Vision | #31, #32, #33, #34, #35, #24, #29 |
| 37 | 35 | App nativo Expo (aluno/paciente) — Bluetooth + push + **Apple Health + Google Health Connect (expande Device Hub)** | mobile | **done (35a backbone)** 2026-05-18 | 3 schemas (`mobile_app_versions` global read-all + unique platform×version + min_required + sunset + `mobile_push_tokens` unique active por (member,device_id,platform) + soft-revoke + index push_dispatcher hot + `mobile_sessions` refresh 90d hash SHA-256 + device_fingerprint + status active/revoked/expired + check revoked_consistency) + RLS (`0053_mobile_rls.sql`: global SELECT all em app_versions + tenant/member portal em push_tokens/sessions); Server Actions wrapped (`registerPushToken` revoga anterior do mesmo device + INSERT; `listMyPushTokens` portal; `revokeMyPushToken` soft-revoke; `checkAppVersion` público sem auth + compareSemver + retorna minRequired/updateRequired/latestVersion/storeUrl); 2 API Routes (`/api/mobile/auth/register-push` Bearer Sprint 35b + `/api/mobile/auth/check-version` público rate-limited regra 36); ADR 0045 Proposed (Expo managed + React Native + TypeScript + Expo Router file-based + config plugins HealthKit/HealthConnect/BLE + 5 alternativas rejeitadas Flutter/bare/CLI/Capacitor/native-separate); ADR 0046 Proposed (App Store + Play Store oficial + OTA via EAS Update JS-only + force update via min_required + access 1h+refresh 90d single-use rotation + push direto APNs HTTP/2 + FCM HTTP v1 sem Expo Push Service regra 46 + EAS Build cloud + 7 alternativas rejeitadas). Migration `0040_mobile_app_backbone.sql`. **Sprint 35b/c:** scaffold `apps/mobile/` Expo SDK 52 + Expo Router v3 + login magic link + checkin QR + push dispatcher direto APNs/FCM em `packages/notifications/` + tabela `push_dispatches` audit + Apple Health (`react-native-health` config plugin) + Health Connect (`react-native-health-connect`) sync diário via background-fetch → POST `/api/mobile/health/sync` expande Device Hub Sprint 32 + UI super-admin pra `mobile_app_versions` + rota `/api/mobile/auth/refresh` rotation single-use + family chain detection + universal links + Apple Smart App Banner + Play Integrity API + RIPD app-mobile + DPO sign-off + feature flag `mobile_app_v1` + E2E Maestro/Detox + EAS Submit App Store Connect + Play Console staged rollout 5%→100% 7d + Apple Phased Release | MVP estável + #28 + #34 |
| 38 | [36](sprints/36-geral-fiscal-focus-nfe.md) | **Módulo fiscal — emissão completa via Focus NFe** (NFS-e + NF-e produto + NFC-e + devolução + transferência + remessa conserto + entrada própria + eventos) — ADR 0059 | fiscal | **doing (36a backbone 2026-05-18 · 36b.1-36b.7 2026-07-19 — escopo desbloqueado esgotado; aguarda débitos de schema + conta Focus)** | 5 schemas + RLS (`fiscal_emissions` 8 kinds × status workflow + provider plug-in ADR 0076 column + source polimórfico + numeracao serie+numero atomic + chave SEFAZ 44dig unique + provider_ref unique + 6 checks consistency + retry queue partial index + @volume 3.6M+/ano; `fiscal_events` append-only kind cancellation/cce/inutilizacao + check `emission_id-XOR-inutilizacao`; `fiscal_numbering_sequences` unique company+kind+serie+env + atomic UPDATE...RETURNING via reserveNextNumero FOR UPDATE; `fiscal_service_catalog` LC116/2003 + ISS basis points 0-500bp + 4 regimes tributários; `fiscal_provider_credentials` PK composta + api_token AES-256-GCM cifrado nonce+tag + webhook_secret cifrado + last_validated_at health check; RLS por tenant + permissions fiscal.read/emit/cancel/admin + portal contador role + role system pra webhook + RLS extra fiscal.admin-only em credentials); migration `0041_fiscal_focus_nfe.sql`; 3 libs puras em `@repo/ai/fiscal` (provider abstract `FiscalProvider` 6 métodos + 4 input schemas TS + 3 result types; `MockFiscalProvider` determinístico SHA-256 gera chave SEFAZ fake 44 dígitos; `cfop-resolver` puro 8 kinds × interno/interestadual com 18 CFOPs canônicos + 5 merchandise kinds + NFC-e SEFAZ-imposed 5102 + NFS-e sentinel 0000 + exhaustive check TS + CANONICAL_CFOPS catálogo readonly UI) com **31 unit tests verdes** (nfse sentinel + nfe industrialized/own_production/fixed_asset/service_input/default + nfce + nfe_return + nfe_transfer + nfe_conserto_out/return + nfe_self_entry + determinismo 10x + CANONICAL_CFOPS catálogo + 6 pares interno/interestadual coverage); 7 Server Actions wrapped (`listEmissions`/`getEmission`/`emitNfseFromInvoice` valida invoice.status=paid + reserveNextNumero FOR UPDATE + persists payload jsonb completo + cancelDeadlineAt 24h; `cancelEmission` MFA gate `cancelNfe` regra 43 valida janela + transação atomic; `issueCce` MFA gate `issueCce` HIGH_RISK_ACTIONS adicionado Sprint 36a + max 30 por chave + só NF-e modelo 55; `inutilizeRange` MFA gate `inutilizeRange` adicionado + valida numeroFrom≤numeroTo; `queryEmissionStatus` re-consulta provider + atualiza local; `retryEmission` rejected retry_count<3 incrementa + zera reason); UI 2 rotas (`/app/fiscal` inbox com 4 KPIs (autorizadas/pendentes/rejeitadas/total BRL) + filtros GET kind+status + tabela 100 últimas com badge `var(--ev-*)` colorido por status + rejection_reason truncado + retry count + link Detalhe; `/app/settings/fiscal` wizard esqueleto 4 Steps marcados done/pending com check verde ou número cinza); HIGH_RISK_ACTIONS expandido com `issueCce` + `inutilizeRange` categoria fiscal. **181 tests @repo/ai verdes** (era 150 +31 cfop-resolver). Typecheck 11/11 packages verde. **36b.1 entregue 2026-07-19:** FocusNfeProvider real (safeFetch regra 37 + allowlist 2 hosts Focus + Basic auth + ref determinística `lf-{kind}-{cnpj}-{serie}-{numero}` idempotente + status map + erros tipados 429/auth/5xx; 400/422→rejected) + payload builders nfse/nfe (6 kinds modelo 55 via natureza+finNFe+CFOP+notas_referenciadas, tax defaults Simples CSOSN 102 overridable)/nfce (formas_pagamento + CPF opcional) com 29 unit tests + `emitNfce` na interface + `kind` em cancel/queryStatus (roteia recurso) + crypto columnar `encryptSecretParts/decryptSecretParts` @repo/security (5 tests) + `resolveFiscalProvider(tenantId)` decifra AES-256-GCM + **mock bloqueado em produção** + CRUD credenciais wizard (token write-only + webhook secret one-time + testar conexão healthCheck) gate `fiscal.admin` + webhook callback token timingSafeEqual + idempotente + no-downgrade + 401 uniforme + flag `fiscal_focus_v1` migration 0054 (disabled default) gate 6 SAs emissão/evento + **permissions fiscal.read/emit/cancel/admin seedadas (gap 36a — RLS referenciava catálogo vazio)** + **fix bug 37b: requirePermission recebia auth_user.id em vez de users.id → apuração FORBIDDEN pra todos; corrigido `session.logifit.userId`** + validado E2E dev (credencial cifrada + secret one-time + webhook 401). 438 tests verdes pacotes tocados. **36b.2 entregue 2026-07-19:** `resolveCompanyFiscal` (CNPJ via persons regra 22 + IM) + `resolveServiceCatalog` (municipality/lc116/cnae/issRateBp reais na NFS-e; erro acionável se catálogo vazio) eliminam placeholders + `NfseEmissionInput.inscricaoMunicipal/issRetido` + rota `/app/fiscal/[id]` detalhe (badge status + chave + origem + janela cancelamento + eventos + banner rejeição c/ retry count) + `<EmissionActions>` condicionais (cancelar janela aberta/CC-e só modelo 55/retry rejected<3/re-consultar) com prompt dialog regra 45 + validado E2E dev (dialog → SA → `MFA_RECENT_REQUIRED` regra 43 inline na UI). **36b.3 entregue 2026-07-19:** CRUD catálogo de serviços `/app/settings/fiscal/catalogo` (form empresa+IBGE validado+LC116 X.YY+CNAE normalizado+regime+ISS % ↔ bp range 2-5%; editar/desativar sem DELETE físico; 3 SAs gate `fiscal.admin`; validado E2E dev — fluxo NFS-e completo destravado). **36b.4 entregue 2026-07-19:** SA `emitNfseManual` (avulsa — caso Solo; company + serviço do catálogo + tomador digitado + valor; sourceKind manual) + form `/app/fiscal/emitir/nfse` (serviços filtrados por company + redirect pro detalhe + botão no inbox) + **permissions RBAC enforced nas SAs** (`fiscal.emit` emitir/retry; `fiscal.cancel` cancelar/CC-e/inutilizar além do MFA regra 43) + validado E2E dev via mock (emissão autorizada + redirect). **Descoberta:** emissão por venda (NF-e/NFC-e) bloqueada — schema de vendas/POS com itens não existe (Sprint 24 entregou só estoque; `nfe_returns` "15b" nunca nasceu) — decidir no roadmap qual sprint cria. **36b.5 entregue 2026-07-19:** `downloadFiscalFile` (safeFetch + Basic auth token decifrado) + route `GET /api/fiscal/emissions/[id]/{pdf|xml}` (sessão + fiscal.read + streaming Content-Disposition + cache 10min + 404 mock-aware) + SAs persistem xml/pdf paths + botões ⬇ no detalhe + **fix regra 44**: primitivos `.ev-*` portados pro app (`packages/ui/base.css` — telas staff renderizavam sem formatação desde Sprint 07). **Gap registrado:** `tenant_usage_snapshots` (Sprint 04/ADR 0066) não existe — job usage snapshot bloqueado. **36b.6 entregue 2026-07-19:** portal contador fase 1 (layout banner "leitura somente" ADR 0061 + gate `fiscal.read` + dashboard c/ seção emissões do mês agregada + `/app/contador/fiscal-emissions` lista 500 notas filtros período/tipo/status + download PDF/XML individual) + palette fiscal (5 rotas + emitir NFS-e). **Gap registrado:** `search_index` (ADR 0062/regra 30) nunca criado — palette segue lista estática. **Sprint 36b.7/c restante:** fontes de emissão pendentes de schema + person picker + cbos-cnae-resolver + IP allowlist runbook + ZIP em massa XMLs + retenções + botões emissão contextualizados em invoices/vendas/POS/intercompany/equipment_maintenance/billing_guides + portal contador `/app/contador` (ADR 0061) + `/app/fiscal/retencoes` por tributo + job aggregate-fiscal-usage-snapshot (ADR 0066) + cron validate-fiscal-credentials semanal + gap detection diário + permissions RBAC + pesquisa global ADR 0062 + feature flag `fiscal_focus_v1` + lints `fiscal-emission-must-have-provider` + `prod-blocks-mock-provider` + E2E Focus sandbox 8 tipos + 3 eventos + negociação comercial Focus NFe `docs/contratos/focus-nfe.md` (R$ 0,12/nota >10k/mês) | #6, #18 (15 schemas), #20 (17 inbox), #24 (POS), #25 (equipamento) |
| 39 | pós-35 | **Prescrição adaptativa IA por RPE** (ajuste de carga automático) | academia/ia | futuro | #13, #37 (app nativo com input RPE em tempo real) |
| 40 | [37](sprints/37-fiscal-apuracao-mensal-receita.md) | **Apuração mensal de receita (Grupo C — ADR 0061 + [ADR 0100](decisions/0100-apuracao-fiscal-mensal-grupo-c.md))** — consolida receita por regime Simples/Presumido/Real/MEI + gera memorial "pré-DAS"/"pré-DARF"; sem emissão oficial | fiscal | **done (37a core + 37b produção-ready)** 2026-05-23 | **37a 2026-05-23**: 3 schemas + 12 brackets Simples Anexos III+V seedados + RLS + trigger bloqueia closed UPDATE + libs puras `@repo/ai/fiscal/apuracao` (calculateSimples/Presumido/Real/MEI + computeAggregation dispatcher + buildMemorial) + **24 unit tests verdes** + 5 SAs wrapped (aggregate/get/list/regenerate/close) + 2 rotas UI (hub `/app/fiscal/apuracao` + detalhe `/[id]`) + memorial estruturado jsonb canônico + snapshot tax_regime + ADR 0100 Proposed. **37b 2026-05-23**: feature flag `fiscal_apuracao_v1` ADR 0098 gate (rota + 5 SAs) + 3 permissions RBAC `fiscal.apuracao.{read,write,close}` ADR 0019 (tenant_owner/gerente write, contador_externo read, super_admin todas) + helper TS `requirePermission` SQL `has_permission()` wrap + cron `aggregate-fiscal-monthly` dia 5 mensal idempotente com counters logging + memorial PDF render server via @react-pdf/renderer A4 retrato (KPIs + breakdown + memorial + rodapé legal) substitui stub + dispatch cross-alert teto Simples warning ≥87%/critical ≥95% via system_alerts ring buffer + banner inline detail page color-coded. **Sprint 37c:** Lucro Real completo cost_entries + E2E Playwright + régua Sprint 13 vencimento DAS + RIPD + DPO sign-off + pesquisa global ADR 0062 + branding tenant PDF + reabrir closed via super_admin. |
| 41 | 38 | **Guias oficiais DAS/DARF/DAM (Grupo D — ADR a alocar ≥0089)** — integração PGDAS-D + geração DARF com código de receita; opcional integração Contabilizei/Conube/Omie | fiscal | **futuro (pós-produção)** | #40 |
| 42 | 39 | **Obrigações acessórias SPED/ECD/ECF/DCTF-Web/DEFIS/DIRF (Grupo E — ADR a alocar ≥0089)** — motor ou delegação a provider tributário (SCI/Alterdata/Domínio); **alta complexidade** | fiscal | **futuro (avaliar make vs buy)** | #40, #41 |
| 43 | 40 | **Folha CLT + eSocial (Grupo F — ADR a alocar ≥0089)** — folha completa (salário, horas, DSR, férias, 13º, rescisão) + INSS patronal + FGTS + IRRF + eventos eSocial S-1000 a S-5013; **muito complexa** — provável integração TOTVS/Senior/ADP vs motor próprio | rh | **futuro (avaliar make vs buy)** | — |

---

## Débitos de schema — dependências fantasma (auditoria Sprint 36b, 2026-07-19)

Durante o Sprint 36b, 6 estruturas citadas como "entregues" ou "esperadas" por sprints anteriores se revelaram **inexistentes no schema**. Docs de sprint referenciam essas tabelas como se existissem — qualquer feature que dependa delas está silenciosamente bloqueada. Decisão do fundador: alocar cada uma a um sprint dono (ou cortar do escopo).

| # | Estrutura ausente | Sprint que "deveria" ter criado | O que bloqueia hoje |
|---|---|---|---|
| 1 | Tabela de vendas/POS com itens + pagamentos | Sprint 24 (entregou só estoque) | Emissão NF-e produto + NFC-e (4 dos 8 kinds do ADR 0059) |
| 2 | `nfe_returns` (ADR 0058) | "Sprint 15b" (nunca existiu) | Emissão NF-e devolução |
| 3 | `tenant_usage_snapshots` (ADR 0066) | Sprint 04 | Job `aggregate-fiscal-usage-snapshot` → cobrança de overage por nota |
| 4 | `search_index` + FTS (ADR 0062, regra 30) | Sprint 07 Faixa D | Pesquisa global real — Command Palette segue lista estática |
| 5 | `tax_natures` / `tax_retentions` por tributo (ADR 0061) | "Sprint 15b" | Relatório `/app/fiscal/retencoes` por tributo (AP/comissões só têm agregado `retention_total_cents`) |
| 6 | Tabela de invites de staff (`createContadorInvite`) | Sprint 01b | Convite de contador externo via magic link + `/app/settings/contador` |

## Decisões pendentes (viram ADRs quando resolvidas)

- Hardware da catraca + modalidade de auth (QR / facial / ambos) — Sprint 08 (ADR 0018)
- DSL de regras de conquista — Sprint 09 (parte do ADR 0021)
- ~~Provider WhatsApp (Twilio / Z-API / Meta direto) — Sprint 13~~ — **resolvido** pelo [ADR 0025](decisions/0025-provider-whatsapp.md) (BSP oficial Meta — POC Twilio vs Gupshup no Sprint 13; Z-API rejeitado por risco de ban)
- **Motor de rateio + modelo de intercompany** — Sprint 16 (ADR 0036)
- **Provider Open Finance (Pluggy / Belvo / API direta) + provider NF-e recepção (Arquivei / Sieg / SEFAZ direto)** — Sprint 17 (ADRs 0037 e 0038)
- ~~**Adquirência: ordem de integração (Cielo/Stone/Rede/GetNet/PagSeguro) + antecipação**~~ — **resolvido** pelo [ADR 0039](decisions/0039-adquirencia-provider-abstrato.md) Proposed (interface abstrata `AcquirerAdapter` + ordem Stone→Cielo→Rede→GetNet→PagSeguro + antecipação manual default + split em runtime). Promove pra Accepted quando POC Sprint 18b com Stone sandbox validar.
- ~~Modelo de churn (API externa / local / serviço dedicado) — Sprint 19~~ — **resolvido** pelo [ADR 0027](decisions/0027-estrategia-modelo-churn.md) (Família A baseline LLM Gemini Flash → Família B sklearn pós-3 meses de dados)
- Provider assinatura digital ICP-Brasil (Cert.Sign / Bry / Vaultsign) — Sprint 20 (parte do ADR 0041)
- Submissão de guia TISS (manual vs automática SOAP) — Sprint 22 (parte do ADR 0042)
- ~~Tributação em comissões (INSS/IR retidos ou só bruto) — Sprint 23~~ — **resolvido** pelo ADR 0061 (motor de retenções em AP e comissão/RPA)
- Método de custo de estoque (PEPS vs custo médio) — Sprint 24 (ADR 0044)
- Integração Datasus CNES automática vs manual — Sprint 25
- Teleconsulta provider (Daily.co / Whereby / Jitsi / Twilio Video) — Sprint 31 (ADR 0045)
- Fonte de valores de referência laboratorial (SBAC / curado LogiFit) — Sprint 30 (parte do ADR 0046)

## Decisões já fechadas (recente)

- **OCR de boleto: provider abstrato configurável** — ADR 0035 accepted (OCR.space default + Google Vision + AWS Textract + Azure + Tesseract)
- **Cadastro central `persons` (Contact-FK)** — ADR 0047 accepted (substitui duplicação entre members/leads/suppliers/companies/users)
- **Busca automática de CNPJ** — ADR 0048 accepted (BrasilAPI default + ReceitaWS fallback + CNPJá! opcional; cache 7d)
- **Device Hub (wearables + clínicos)** — ADR 0049 accepted (padrão FHIR-like Observation + provider abstrato; Garmin/Oura + BLE bioimpedância + import FIT/CSV no Sprint 32; Apple Health + Google Health Connect no Sprint 37 App Nativo)
- **Pipeline inteligente de exames laboratoriais** — ADR 0050 accepted (OCR → IA extração → IA interpretação conservadora → revisão profissional → `lab_results` oficial; paciente sobe via portal com fila de revisão; nunca diagnostica; classificador de output bloqueia termos proibidos)
- **DPO + Governança Compliance LGPD** — [ADR 0067](decisions/0067-dpo-governanca-compliance-lgpd.md) accepted (DPO interno fundador no MVP; `privacidade@logifit.com.br`; plano resposta 72h; sub-processors públicos; auditoria trimestral)
- **Plano comercial / pricing / trial** — [ADR 0066](decisions/0066-plano-comercial-pricing-trial.md) accepted (revisado 2026-04-25 — 4 tiers principais + Solo/Combo + cota IA hard-stop + NFS-e inclusas + overage)
- **NFS-e Padrão Nacional como provider complementar futuro** — [ADR 0076](decisions/0076-nfse-nacional-provider-complementar.md) accepted 2026-04-25 (não substitui Focus NFe; pós-Sprint 36 estável 3 meses + 10k notas/mês + 30% emissões em municípios aderidos)
- **Passaporte do paciente (vínculo cross-tenant)** — [ADR 0077](decisions/0077-passaporte-paciente-vinculo-cross-tenant.md) accepted 2026-04-25 (Modelo C híbrido + 5 módulos canônicos + 5 níveis de dados + audit log particionado)
- ~~**Hospedagem em duas fases (MVP Supabase → Pós-MVP Oracle Cloud OCI)**~~ — ~~[ADR 0078](decisions/0078-hospedagem-duas-fases-mvp-supabase-pos-mvp-oracle.md)~~ **superseded por [ADR 0091](decisions/0091-self-host-total-oracle-sp.md)** (2026-04-27)
- **Self-host total Oracle Cloud SP + Coolify + Postgres + Caddy desde Sprint 00** — [ADR 0091](decisions/0091-self-host-total-oracle-sp.md) accepted 2026-04-27 (supersede ADR 0078; pula fase Vercel+Supabase; deleta Sprint 19b; cria regra 46 — toda dependência externa nova exige ADR)
- **TISS 4.01 ANS padrão vigente** — [ADR 0079](decisions/0079-tiss-401-ans-padrao-vigente.md) accepted 2026-04-25 (Ofício-Circular ANS nº 1/2026; pipeline atualização semestral; ADRs 0029/0030/0031/0042 detalham implementação)
- **Lib `qrcode` pra render QR TOTP (regra 46)** — [ADR 0095](decisions/0095-qrcode-lib-mfa-totp.md) accepted 2026-05-20 (alternativa SVG Reed-Solomon próprio rejeitada por complexidade; render server-side via wrapper isolado `generateTotpQrSvg`; zero bundle JS no client)
- **Brevo substitui AWS SES como provider de email transacional** — [ADR 0096](decisions/0096-email-brevo-substitui-aws-ses.md) accepted 2026-05-20 (substitui parcialmente ADR 0091 §"Email transacional: AWS SES"; tier free 300/dia cobre MVP sem custo; provider abstrato em `@repo/email` futuro; um sub-processor AWS a menos; mantém Cloudflare como provider multi-uso)
- **Email sender por categoria + branding por tier comercial** — [ADR 0097](decisions/0097-email-sender-categoria-branding-por-tier.md) accepted 2026-05-20 (complementa ADR 0096; 2 categorias canônicas — Categoria 1 plataforma sempre LogiFit, Categoria 2 tenant com fallback Solo/Starter ou domínio próprio Pro+/Business ou white-label Enterprise; schema `tenant_email_settings` + cron `verify-email-domains` + DNS verification via Brevo)
- **Feature flag system MVP self-host** — [ADR 0098](decisions/0098-feature-flags-mvp-self-host.md) accepted 2026-05-21 (tabela própria `feature_flags` + helper `isFeatureEnabled` com cache 60s + fail-closed; 14 flags canônicas seedadas; GrowthBook/LaunchDarkly rejeitados por complexidade desproporcional MVP; UI admin + audit_log integration ficam Sprint 02b7+)
- **WhatsApp integração Nível 1 (wa.me link) único no MVP** — [ADR 0099](decisions/0099-whatsapp-nivel-1-wa-me-mvp.md) accepted 2026-05-21 (Nível 2 Cloud API bot + Nível 3 IA conversacional fora do escopo MVP; `wa.me` link gratuito + zero LGPD impact + zero setup Meta; thresholds claros pra promoção futura — >200 signups/dia sustentado + conversion <35% por 14d; roadmap migração 2-3 semanas documentado)

## Decisões já fechadas

- **OCR de boleto: arquitetura provider-abstrato configurável pelo admin do tenant** — ADR 0035 accepted no Sprint 15. Default global: OCR.space (tier gratuito 25k/mês); alternativas suportadas: Google Vision, AWS Textract, Azure Computer Vision, Tesseract self-hosted. Admin configura via `/app/settings/financeiro/ocr` com credentials próprias; fallback em cadeia opcional.

---

## Convenção sobre sprints em alto nível

Sprints **34** (Nutri-Agent IA), **35** (App Nativo Expo), **pós-35** (Prescrição Adaptativa IA por RPE), **37** (Apuração Receita), **38** (Guias DAS/DARF), **39** (Obrigações Acessórias SPED) e **40** (Folha CLT + eSocial) aparecem na tabela da Fase 3 **apenas como entrada de roadmap** — não têm arquivo detalhado em `docs/sprints/NN-*.md` ainda. Isso é **deliberado**: arquivo de sprint profundo nasce quando o sprint vira candidato a `doing` (próximos 1-2 na fila). Evita doc especulativa que fica obsoleta antes de executar. Quando chegar a hora, o arquivo é criado via mesmo template + extensão (formato profundo: Goal, Aceite, Dependências, Schemas, Rotas, Server Actions, Eventos, Commit checklist, DoD).

Sprints **36** (Fiscal Focus NFe) **possui** arquivo detalhado já — foi escrito com profundidade pra orientar planejamento da decisão fiscal (ADR 0059). O fato dele existir antes dos 34/35 não viola a convenção — apenas reflete a sequência cronológica de quando precisaram ser planejados.

> **Nota de leitura:** quando uma célula da coluna "Sprint" mostra apenas o número (ex: `34`, `35`, `37`) sem link markdown, isso significa que ainda não há arquivo `docs/sprints/NN-*.md` correspondente. Não é link quebrado — é placeholder intencional conforme convenção acima.

## Convenção de numeração de ADRs

ADRs no LogiFit seguem **numeração sequencial densa** com uma exceção legítima: **a faixa 0011-0046 está reservada para ADRs que serão produzidos durante a execução de sprints específicos**. Cada sprint planejado lista os ADRs que vai produzir (campo "ADRs esperados" no header do sprint), e o número é alocado naquele momento.

**Por quê?** Sprints capturam decisões arquiteturais que só fazem sentido **depois** que se entende o domínio do sprint (ex: "qual provider WhatsApp escolher" só decide quando Sprint 13 começa). Pré-criar ADRs vazios polui `docs/decisions/` e força decisões prematuras.

**Mapeamento atual** (faixa reservada):
| ADR | Sprint que produz | Tema | Status |
|---|---|---|---|
| 0011-0012 | reservados | (não-alocados; gap legítimo histórico) | livre |
| 0013 | Sprint 04 | Plano/Contrato/Cobrança como entidades separadas | a produzir |
| 0014 | Sprint 04 | Chave Asaas + conta bancária por company vs tenant | a produzir |
| [0015](decisions/0015-copilot-safety-classifier-dois-pontos.md) | Sprint 06 | Copilot safety: classifier de input + output em dois pontos com regex curado | **Accepted (formalizado 2026-05-18)** |
| 0016-0017 | reservados | (futuros, allotment livre) | livre |
| 0018 | Sprint 08 | Hardware da catraca + modalidade de auth | a produzir |
| 0019 | Sprint 01b | RBAC: union user_roles + user_permissions com expires_at | a produzir |
| 0020-0024 | reservados | (futuros, allotment livre) | livre |
| [0025](decisions/0025-provider-whatsapp.md) | Sprint 13 | Provider WhatsApp (BSP oficial Meta — Twilio/Gupshup; Z-API rejeitado) | **Proposed (formalizado 2026-04-27)** |
| [0026](decisions/0026-motor-regua-dsl.md) | Sprint 13 | Motor declarativo de régua DSL JSON (`trigger`/`actions`/`stop_on`/`guards`) | **Proposed (formalizado 2026-04-27)** |
| [0027](decisions/0027-estrategia-modelo-churn.md) | Sprint 19 | Estratégia modelo churn (Família A LLM baseline → Família B sklearn Fase 2) | **Proposed (formalizado 2026-04-27)** |
| [0028](decisions/0028-cid-cif-catalogos-globais.md) | Sprint 20 | CID-11 + CIF como catálogos globais versionados curados LogiFit | **Proposed (formalizado 2026-05-17)** |
| [0029](decisions/0029-tiss-tuss-schema-xml-generator.md) | Sprint 22 | Estrutura TISS/TUSS: 11 schemas + gerador XML 4.01 customizado | **Proposed (formalizado 2026-05-17)** (detalha ADR 0079) |
| [0030](decisions/0030-tuss-update-pipeline.md) | Sprint 22 | Pipeline atualização semestral terminologia ANS (manual MVP, jul/2026 primeira) | **Proposed (formalizado 2026-05-17)** (detalha ADR 0079) |
| [0031](decisions/0031-tiss-validador-proativo.md) | Sprint 22 | Validador TISS proativo 10 regras negócio (XSD diferido Sprint 22b) | **Proposed (formalizado 2026-05-17)** (detalha ADR 0079) |
| [0032](decisions/0032-assinatura-prontuario-por-profissao.md) | Sprint 20 | Política de fechamento de prontuário por profissão (`signature_policies` ICP-Brasil/lacre autenticado) | **Accepted (formalizado 2026-04-27)** |
| 0033-0034 | reservados | (futuros) | livre |
| [0035](decisions/0035-ocr-boleto-provider-abstrato.md) | Sprint 15 | OCR de boleto: provider abstrato (OCR.space default + Google Vision + AWS Textract + Azure + Tesseract) | **Accepted (formalizado 2026-04-25)** |
| 0036 | Sprint 16 | Motor de rateio + intercompany | a produzir |
| 0037 | Sprint 17 | Provider Open Finance | a produzir |
| 0038 | Sprint 17 | Provider NF-e recepção | a produzir |
| [0039](decisions/0039-adquirencia-provider-abstrato.md) | Sprint 18 | Adquirência: provider abstrato + ordem de integração + antecipação manual | **Proposed (formalizado 2026-05-17)** |
| 0040 | reservado | (livre — Sprint 19 churn migrou para ADR 0027) | livre |
| 0041 | Sprint 20 | Provider assinatura digital ICP-Brasil | a produzir |
| 0042 | Sprint 22 (ou posterior) | Submissão de guia TISS automática SOAP | a produzir (Fase 2 do ADR 0079) |
| 0043 | Sprint 34 | Arquitetura Nutri-Agent (especializado vs generalizado) | a produzir |
| 0044 | Sprint 34 | Política mudança automática plano alimentar (sempre proposta) | a produzir |
| [0045](decisions/0045-stack-mobile-expo-managed-react-native.md) | Sprint 35 | Stack mobile: Expo managed + React Native + TypeScript + Expo Router | **Proposed (formalizado 2026-05-18)** |
| [0046](decisions/0046-release-strategy-app-stores-ota.md) | Sprint 35 | Estratégia de release (App Store + Play Store + OTA EAS Update + push direto APNs/FCM) | **Proposed (formalizado 2026-05-18)** |

**Referências a esses ADRs em outros documentos** (ex: regra que cita "ADR 0015") são **válidas como compromisso de produção** — quando o Sprint 06 rodar, ADR 0015 nasce e o link passa a resolve. Até lá, links como `(ADR 0015)` em corpo de texto são aceitos sem `.md` resolvendo.

**Gate operacional (regra de processo aplicada a TODA sprint):** sprint que cita "ADR XXXX (esperado)" no header **não pode entrar em `status=doing`** sem antes publicar o ADR esperado. Na prática, isso vira o primeiro item do checklist de DoD da sprint:

```markdown
- [ ] ADR XXXX publicado em `docs/decisions/` antes de iniciar
```

Sprints existentes que ainda não declaram esse item explicitamente herdam a regra deste roadmap — auditar quando sprint for transicionar para `doing`.

### Numeração pós-0046 (faixa fora-de-sprint)

A faixa **0011-0046 está reservada** (acima); a faixa **0047+ é alocada sequencialmente** para ADRs **fora-de-sprint** (decisões transversais que não nascem dentro de uma sprint específica — DPO, hospedagem, passaporte, fiscal arquitetural). Atualmente: 0047-0050 (decisões recentes), 0051 livre, 0052-0079 publicados sequencialmente, **0089 publicado** (sistema de mensagens padronizadas — fora da faixa 0080-0088 reservada para sprints 23-31, ver tabela abaixo), **0090 publicado** (estratégia de testes T1-T21), **0091 publicado** (self-host total Oracle SP — supersede 0078), **0092-0099 publicados sequencialmente** (BetterAuth/passport_global_identities/sessions/qrcode/email/feature-flags/WhatsApp), **[0100 publicado](decisions/0100-apuracao-fiscal-mensal-grupo-c.md) Proposed 2026-05-23** (Apuração fiscal mensal Grupo C — Sprint 37 produz). **Próximo ADR fora-de-sprint disponível: 0101+**. Caso um sprint reservado precise produzir ADR mas a faixa 0011-0046 já alocou outro tema correlato, alocar próximo número livre acima de 0080 e referenciar back na tabela acima.

**Realocações da faixa 0011-0046 → 0080+** (auditorias 12, 14 e 15 — 2026-04-25, resolveram colisões herdadas):

| ADR | Sprint que produz | Tema | Por que migrou |
|---|---|---|---|
| **0080** | Sprint 29 | Banco de alimentos TACO + estrutura `foods`/jsonb | 0035 já alocado a OCR boleto (Accepted) |
| **0081** | Sprint 29 | Plano alimentar (`meal_plans` → `meal_plan_meals` → `meal_items`) | 0036 alocado a Sprint 16 rateio intercompany |
| **0082** | Sprint 30 | Suplementação separada de alimentos (`supplements`) | 0037 alocado a Sprint 17 Open Finance |
| **0083** | Sprint 31 | Teleconsulta provider (Daily/Whereby/Jitsi/Twilio) | 0038 alocado a Sprint 17 NF-e recepção |
| **0084** | Sprint 27 | CID → contraindicação de exercício (`cid_exercise_contraindications`) | 0033 alocado a Sprint 15 plano de contas hierárquico |
| **0085** | Sprint 28 | Generative UI fisio (Vercel AI SDK `ui.streamUI` + registro componentes) | 0034 alocado a Sprint 15 workflow AP configurável |
| [**0086**](decisions/0086-modelo-comissao-profissional.md) | Sprint 23 | Modelo de comissão fisio (4 kinds × 4 bases + versionamento + imutabilidade pós-approved) | **Proposed (formalizado 2026-05-17)** — 0030 alocado a Sprint 22 pipeline atualização TISS |
| [**0087**](decisions/0087-estoque-custo-saldo-model.md) | Sprint 24 | Método de custo estoque (PEPS + custo médio configurável por item) + modelo de saldo (SUM não denormalizado) + APPEND-ONLY | **Proposed (formalizado 2026-05-17)** — 0031 alocado a Sprint 22 validador TISS proativo |
| **0088** | Sprint 26 | Autenticação do member (magic link + JWT `role=member` + sessão 30d) | 0032 alocado a Sprint 20 política de fechamento de prontuário |

## Operação

- Quando um sprint começa: mudar `status → doing`, preencher `Início`; regra 9 (1 `doing` por vez)
- Durante: atualizar `%` (0/25/50/75/100) quando bater cada 25% do checklist Commit
- Se bloquear: `status → blocked`, preencher `Bloqueios` com 1 linha + data do bloqueio
- Ao fechar: `status → done`, preencher `Fim`, `PR`, validar DoD
