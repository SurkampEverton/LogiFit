# RIPD v1.0 — Portal do paciente web (acesso ao próprio prontuário)

> **Stub** — RIPD esperado pela regra 29 + ADR 0054 para o portal `/meu/*` entregue em [Sprint 26](../../sprints/26-geral-portal-paciente-web.md). Será expandido quando a Sprint 26 entrar em `doing` (template-base em [`_template.md`](_template.md)).

- **Versão:** v1.0 (stub)
- **Próxima revisão obrigatória:** definir na entrega do Sprint 26 (semestral conforme regra 29)
- **Parecer DPO:** **pendente** — gate de produção não libera sem parecer

## 1. Identificação do tratamento (rascunho)

- **Nome do módulo:** Portal do paciente web — `/meu/*` (acesso titular ao próprio prontuário, avaliações, prescrições, histórico de cobrança, consents, exercícios do dia, alertas IA)
- **Sprint que entrega:** Sprint 26
- **ADR de referência:** ADR 0088 (esperado — autenticação member via magic link; ver Sprint 26)
- **Finalidade declarada:** atender LGPD art. 18 (direitos do titular) com portal nativo + UX simplificada para o paciente

## Notas críticas (a expandir)

- **Não é tratamento novo** — é canal de exercício de direitos sobre tratamentos existentes (prontuário, avaliações, prescrições, cobrança). RIPD documenta o canal + controles.
- Autenticação separada do namespace operador (role `member`, JWT distinto, sem permissions de operador) — ADR 0088 esperado
- Sessão longa 30d com revogação remota; setup de MFA opcional mas incentivado
- Auditoria: cada acesso do member a uma tela `/meu/*` grava `audit_log` com `actor=member` (cadeia hash mantida — regra 39)
- Direitos cobertos pelo portal: confirmação, acesso, correção, anonimização (com gate DPO), portabilidade (JSON + PDF), revogação de consent, informação sobre compartilhamento, revisão de decisão IA (regra 28)
- Cross-tenant: tab "Outras Empresas" no portal do paciente (ADR 0077) — leituras gravam `patient_data_access_log`

## 9. Anexos

- Pendente.
