# Dependências
node_modules/
.pnpm-store/
.yarn/

# Build outputs
dist/
build/
.next/
.turbo/
out/

# TypeScript
*.tsbuildinfo

# Testes / coverage
coverage/
.nyc_output/

# Ambiente
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Claude Code — local settings (não versionar)
.claude/settings.local.json

# Docker dev volumes (Postgres + Redis + MinIO + Mailhog + GlitchTip)
.docker-data/

# Logs
*.log
logs/
npm-debug.log*
pnpm-debug.log*

# Drizzle migrations geradas (snapshots — versionar; introspecção temporária — não)
packages/db/drizzle/meta/_journal.tmp.json

# Backup local de dumps Postgres (uso pontual)
*.sql.gz
*.dump

# Arquivos temporários do agente
.git/COMMIT_MSG_TMP
